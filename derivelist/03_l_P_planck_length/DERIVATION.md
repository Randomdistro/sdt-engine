# ℓ_P — Planck length (Axiom R1 — the one dimensional seed)

> **J. C. Harvey, Melbourne — 2026-07-30.** Adopted value: 1.616255×10⁻³⁵ m.
> derivation basis entry: `laws.hpp:112-124` (full provenance block in-header).
> Primary record: FLM06 (`Investigations/01_Foundations_and_Lattice_Mechanics/FLM06_Spation_Scale_Closure/`).
> Re-run: `../derivelist_results.txt` §[4].

## Role in the framework

ℓ_P is the spation floor — the lattice cell size. Load-bearing everywhere:
N = R_CMB/ℓ_P, P_conv = Φ/ℓ_P³, S_boundary = 4πN², the koppa bridge.

## What was attempted (FLM06, executed June 2026)

**Phase 1 — Clearing subdivision (NEGATIVE).** Target: an ℏ/G/ℓ_P-free count N
with R_CMB/N = ℓ_P, i.e. N ≈ 5.894×10⁶¹. Every clean SDT-native candidate
(z_rec, T_rec/T_CMB) lands at ~10³ — short by a factor of **5.36×10⁵⁸**
(re-run confirms). The candidates that reach 10⁶¹ do so only by smuggling ℓ_P
(Planck ticks) or ℏ (thermal length) back in. **Route excluded.**

**Phase 2 — the Seed Theorem (the real result, PROVEN).** From the SDT-native,
ℏ/G-free quantity set {R_CMB, c, k_B, T_CMB, T_rec, z_rec}, no dimensionless
combination of order 10⁶¹ exists. Every length that could subdivide R_CMB
(thermal, Compton, Planck) requires an action quantum or a mass to exist.
**Exactly one action/mass/length anchor is mathematically unavoidable — ℓ_P is
it.**

**The koppa re-expression (circular, printed as such).**
ℓ_P = √(ϟ_per_baryon · ƛ_p) reproduces ℓ_P exactly (re-run: dev +0.0000%), but
koppa_per_baryon is *defined* as ℓ_P²·c·m_p/ℏ — the identity re-homes the seed
from the borrowed pair {ℏ-as-QM, G} to one SDT-native length + the integer W+1.
A re-expression, not a derivation (`laws.hpp:117-123` states this in-header).

## Scope / Limitations / Residuals

- The Seed Theorem is a dimensional-analysis result over the stated quantity
  set; it does not exclude a future *counting* argument that produces 10⁶¹
  natively — none exists in the repo today.
- The standard-physics relation G = ℓ_P²c³/ℏ is a consequence of the derivation basis,
  not a source (`Theory/02_Inputs_and_Derivations.md:26`).

## Status

**NO line drawn — and that is the theorem.** ℓ_P stays Axiom R1, the one
irreducible dimensional seed the framework is permitted. Crossing it off is
mathematically excluded (FLM06 Seed Theorem); the derivation basis cannot compress
below one ruler, and this is the ruler.
