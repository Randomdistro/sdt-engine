# e — elementary charge (irreducible unit bridge)

> **J. C. Harvey, Melbourne — 2026-07-30.** Value: 1.602176634×10⁻¹⁹ C (SI exact,
> definitional since 2019). Whitelist entry: `laws.hpp:131-135` (with the EMC02
> ruling in-header). Primary records: EMC02
> (`Investigations/03_Electromagnetism/EMC02_Elementary_Charge_Quantisation/EMC02_VERDICT_DIRECT_2026-07-23.md`),
> EMC03 (ontology), EMC04 (the derived target). Re-run: `../derivelist_results.txt` §[9].

## Role in the framework

There is no charge substance. "Charge" is handed redirection of the influx
(EMC03: repulsion = surplus/focused, attraction = deficit/shadow; two signs
because redirection is handed, where gravity's convergence has one). e is the
**unit bridge** that converts redirection counts into coulombs — the same class
of object as k_B.

## What is on record (EMC02, direct re-run 2026-07-23)

**The dimensional result.** The whitelist {ℓ_P, c, ℏ, k_B, T_CMB} contains no
current dimension [A]; no algebraic combination yields coulombs. Confirmed by
the direct run. e is an **irreducible Tier-1 measured input**.

**The three candidates, adjudicated:**

- Candidate 1 (P_conv×ℓ_P⁴/ε₀): **excluded** — dimensionally incorrect and
  circular (ε₀ encodes e).
- Candidate 2 (e = e₀·√Lk): **deferred** — dimensionally sound; blocked on the
  hydrogen linking number (PPT09) and a lattice-derived e₀, both open.
- Candidate 3 (e = √(αℏc/k_e)): exact to 3×10⁻⁸ % but **circular** — the
  Coulomb identity is definitional. Re-run:

```
e = sqrt(αℏc/k_e) = 1.6021766335e-19 C   (dev −0.0000%)   [CIRCULAR, printed as such]
```

**The physical target behind e is derived.** "Derive e" is a category error
(a unit is chosen, not derived); the live physical object is the redirection
magnitude — and EMC04 derives it on framework terms:

```
P_eff = m_p²m_e²c⁵/(4παℏ³) = 5.2273e+31 Pa   (dev +0.04% vs B22 anchor)   [DERIVED]
```

with the electropause kinematics closing F = m_e·v²/a₀ = Coulomb (delete-test
passes; α the one remaining input in the expression).

## Scope / Limitations / Residuals

- Since 2019, e is SI-exact — it defines the coulomb/ampere. As with k_B, the
  numeral is a unit choice.
- Candidate 2 remains the only conceivable derivation shape (topological
  linking); it stays deferred behind PPT09 and PPT02, both open.

## Status

**Line drawn — by reclassification.** e is a unit bridge over a mechanism that
carries no charge substance; its numeral is definitional, its dimension is
unreachable from the whitelist (structural negative, on the record), and the
genuine physical magnitude (P_eff) is derived. Nothing mysterious remains
attached to the symbol.
