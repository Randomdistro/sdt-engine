# ℏ — reduced Planck constant (circulation action of the W=1 vortex)

> **J. C. Harvey, Melbourne — 2026-07-30.** Measured anchor: 1.054571817×10⁻³⁴ J·s
> (SI exact via h). derivation basis entry: `laws.hpp:111`. Primary record: FLM13
> (`Investigations/01_Foundations_and_Lattice_Mechanics/FLM13_Koppa_Route_to_the_Action/`).
> Re-run: `../derivelist_results.txt` §[3].

## Role in the framework

ℏ is the **Onsager–Feynman circulation quantum of the fundamental W=1 vortex**
(the electron). It is not a "quantum of action" postulate — it is what one turn
of the unknot's circulation costs, and every dimensionful atomic length in the
catalogue (a₀, r_e, R_p, Compton lengths) encodes it.

## Derivation routes on record (FLM13, executed 2026-07-03)

**P1 — ROLE (earned).** Three forms of the W=1 circulation action agree with ℏ
to ≤10⁻⁶ (gate PASS):

```
m_e·(αc)·a₀   = 1.0545718176e-34   dev +0.0000%   [IDENTITY-CLASS]
r_e·m_e·c/α   = 1.0545718176e-34   dev +0.0000%   [IDENTITY-CLASS]
```

Labelled identity-class on the record: a₀ and r_e are ℏ-defined lengths, so
these derive what ℏ **is** (the circulation quantum), not its number.

**P2 — no ℏ-free value path (dimensional theorem).** Every dimensionful atomic
input encodes ℏ; only α and c are ℏ-free. No combination of lengths, times and
pure numbers manufactures the mass dimension; no ratio is an action. Confirmed
by the FLM13 delete-table.

**P3 — the one-seat collapse (earned).** {ℏ, m_e, m_p} collapse to ONE
mass/action seat:

```
hbar/m_e = κ/2π = 1.1576763605e-4 m²/s     (kilogram-free observable, photon-recoil class)
R_p·m_p·c/hbar  = 4.0007815131  (+0.0195% vs W+1 = 4)
m_p/m_e = (4/α)(r_e/R_p) = 1835.79  (−0.0195%)  — ℏ eliminated entirely
```

The two 0.02% closures are algebraically the same residual seen from both ends —
one consistency fact, not two (stated on the FLM13 record).

**P4 — what the seat is.** The only historically ℏ-free mass anchor was the
artifact kilogram — a counted lump of baryons. The irreducible seat is one
macroscopic **baryon-count anchor**; the ladder distributes it via W=1 and W=3.

## Scope / Limitations / Residuals

- The VALUE of ℏ is not derived and cannot be (P2). What is earned is the role
  and the collapse: three derivation basis entries become one.
- The +0.0195% residual is the muonic R_p measurement against exact W+1=4 —
  the same residual as the pure-ratio mass statement.

## Status

**Line drawn — by collapse.** ℏ leaves the list as an independent mystery; it
is the W=1 circulation action, and its magnitude is the one mass/action seat
shared with m_e and m_p (handed to FLM12). What remains open is the seat's
absolute magnitude, not ℏ separately.
