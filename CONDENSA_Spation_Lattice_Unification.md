# CONDENSA — The Spation Lattice and the Unification of Gravity, Electricity, and Magnetism

> A distillation for review. **J. C. Harvey, Melbourne — 2026-06-23 (v2, hardened after peer audit).**
>
> **The labels are the argument.** Three kinds of "derived" are kept *separate* and must not be conflated:
> - **[GEOMETRY]** — pure mathematics, no physics input. Provable, checkable, closed.
> - **[FORM / MAGNITUDE]** — the *shape* of a physical law is forced; its single coefficient is either **α** (the one seat) or **[OPEN]**.
> - **[IDENTIFICATION]** — a geometric object is *claimed to be* a physical thing (the network *is* the vacuum; the twist *is* charge). Promising, **not closed** — named as such.
> - **[OPEN]** owed · **[KILLED]** tested and abandoned (phrased narrowly).
>
> Quorum is sought on whether the **[GEOMETRY]** rows are exact, the **[IDENTIFICATION]** rows are honestly flagged, and the **[OPEN]/[KILLED]** rows are complete.

---

## 0. Thesis

The proposed vacuum substrate is a **statistically isotropic corner-sharing tetrahedral random network** — the
same topological class as Zachariasen silica-like continuous random networks (CRNs), but **interpreted as a
spation contact-boundary lattice, not atomic matter.** Regular tetrahedral *face/edge* sharing fails by exact
angular frustration; *corner*-sharing converts that frustration into hinge freedom. Gravity, electricity, and
magnetism are then **three motions of this one medium** — scalar compression, chiral twist, moving-twist
circulation — and they share **one coupling magnitude, α** (plus, for the gravitational *floor* only, one
cosmic boundary-state `H₀` — an input of a different kind, *not* a coupling). The forms are geometric; the
single coupling is α.

> The Boerdijk–Coxeter helix is **not** the substrate. It is the **proof that face-sharing fails** — and the
> **natural screw geometry of propagation channels through the CRN voids.** Corner-sharing is the *bulk*; the
> helix is the *channel*. (Stated once so the argument never appears to swap lattices mid-stream.)

---

## 1. The substrate — frustration as a failure proof, corner-sharing as the escape  **[GEOMETRY]**

The lattice fork was: *periodic crystal* (tiles, but anisotropic) **vs** *close-packed icosahedral* (isotropic,
but frustrated). Both are **killed by exact geometry**:

- tetrahedron dihedral `= arccos(1/3) = 70.528779°`
- five around one edge `= 352.643897°` → **deficit `Δ = 7.356103°` per edge** → regular tetrahedra **cannot
  tile Euclidean 3-space** (Conway–Torquato).
- face-stacking therefore produces the **Boerdijk–Coxeter tetrahelix**, twisting `arccos(−2/3) = 131.810315°`
  per cell (irrational → never closes, elongates forever; this is why N = 11 has no convex deltahedron).
- the **same frustration** reappears in the 12-around-1 icosahedral shell as an exact surface gap: outer
  centres sit at circumradius `2r`, icosahedron edge `s = 8r/√(10+2√5) = 2.102924 r`, so neighbour gap
  `g = s − 2r = 0.102924 r`. *(Stated as two expressions of one frustration — **not** as "the deficit summed
  radially equals the gap"; that angular→linear identity is not claimed without the explicit map.)*

**Resolution — corner-sharing, not face-sharing.** The shared *vertex* is a **hinge**; the inter-tetrahedral
angle is free; the network flexes to absorb the 7.356°. The tetrahedra stay locally rigid while the network
flexes globally. Consequences:

| property | value | consequence |
|---|---|---|
| coordination | **4** | `= W+1` (the trefoil/lepton seat) |
| void fraction | **~66%** (diamond-limit node model: `φ = π√3/16 ≈ 0.34`) | open channels the convergence threads |
| symmetry (amorphous form) | **statistically isotropic** | no preferred **axis** (preferred *frame* is a separate debt — §2, §7) |
| space-occupation | **yes** | known precedent: silica-like CRN occupies 3D as a flexible network |

This is the only branch that is **isotropic AND space-occupying at once**. It is a *known* material topology
(window glass, amorphous ice) — proof that the **topology can occupy 3D**, *not* proof that the vacuum *is* it
(that step is **[IDENTIFICATION]**, §7).

---

## 2. The compressible mode = the gravitational redshift  **[MECHANISM: GEOMETRY · IDENTIFICATION: interpretive]**

The tetrahedra are rigid; the network's only give is the tetrahedra **rotating at the hinges** — **rigid-unit
modes (RUMs)**, the measured mechanism behind silica's soft modes and negative thermal expansion.
**Compression = hinge rotation, not squeeze.** Under pressure the hinges close, the spacing `ℓ_P(r)` shrinks:

```
c_local = c_∞ (1 − z) ,    z = ϟ/r = (v/c)² = gR/c²      (dτ/dt_∞ = 1 − z)
```

**The SDT reading is physical (analog), not coordinate (metric).** Near mass the spations are *smaller*, so the
spation-to-spation handoff is *physically slower* (GOM05; Shapiro delay = smaller spations, **not** a metric
well). This **departs from GR's "locally-measured c is always c"** — and the departure is *not* refuted by
local interferometry, because the shrink is **common-mode to light and to every internal ruler/clock**; only
the **inter-regime differential** is observable (the redshift, the Shapiro delay). The departure is therefore a
**falsifiable feature, named not hidden.**

- **[GEOMETRY]** the RUM hinge-rotation soft mode exists and is the network's only low-energy give.
- **[IDENTIFICATION — interpretive]** that this mode *is* the gravitational redshift, with `c_coord = c_∞(1−z)`.

The redshift is one law read by three instruments, anchored on Earth:

| system | g (m/s²) | z = gR/c² | zc (m/s) | read by |
|---|---|---|---|---|
| Earth surface | 9.807 | 6.95×10⁻¹⁰ | 0.21 | Pound–Rebka (✓ 1%) |
| Sun surface | 274 | 2.12×10⁻⁶ | **636** | Shapiro + solar redshift |
| Milky Way @ Sun | 1.9×10⁻¹⁰ | 5.4×10⁻⁷ | — | rotation blue/redshift |

`zk² = 1` is the closure identity (`z<1` local; the **cosmological** redshift is a *product* of octaves, not one
rung — `1+z = 2ⁿ`, recombination `= 10.09` octaves).

**Mode hygiene (mandatory — keeps light from being a sound wave).** The redshift/compression mode and the
EM/light mode are *different eigenmodes of the same network*:

```
c_L² = (κ + 4G/3)/ρ   ← longitudinal/compressive  =  gravity, hinge-closure, redshift
c_T² = G/ρ            ← transverse shear/twist     =  light/EM   (c_EM ≡ c_T)
```

Gravity/redshift lives on the **compressive** branch (hinge closure, `z ∼ Δℓ_P/ℓ_P`); electromagnetism lives on
the **transverse twist/shear** branch. Same medium, two branches — so "you made light a longitudinal sound
wave" does not land.

---

## 3. Gravity — the lattice pushing  **[FORM: GEOMETRY · COEFFICIENT: α · CROSSOVER: OPEN]**

Gravity is **occlusion**: two masses shadow the inward convergence flux from each other (Law III). The form is
**derived from solid angle**:

```
body-1 area πR_occ,1² × sky-fraction blocked R_occ,2²/(4r²) × P_eff
   ⟹   F = (π/4) P_eff R_occ,1² R_occ,2² / r²        (1/r² shared by Coulomb, gravity, nuclear)
```

**`R_occ` is the effective occlusion cross-section, NOT geometric radius** — else a balloon and a lead sphere of
equal size would gravitate equally. `R_occ² = η_ρ R_geom²`, the coherent displacement cross-section (Law IV:
mass = displaced volume, not substance). The **coefficient `P_eff` reduces to the single seat α** (§7).

The low-acceleration floor is the **horizon-circumference handoff rate** of the medium — the weakest nonlocal
relaxation acceleration available when the closure path is the whole cosmic loop `C_H = 2π R_H = 2πc/H₀`. It is
the orbital form `a = v²/R` for a `c`-speed closure around the Hubble loop:

```
a₀ = c² / C_H = c² / (2πc/H₀) = c H₀ / 2π
   = 1.042×10⁻¹⁰ m/s²   (Planck H₀=67.4)   |   1.13×10⁻¹⁰   (SH0ES H₀=73.0)
```

**The load-bearing choice is the `2π` — circumference, not radius.** `a₀ ~ cH₀` is robust to order of
magnitude; the closure-is-the-full-loop assumption is what earns the *exact* factor (the radius instead gives
`cH₀`, 6× too large). The transition radius where local occlusion drops to the floor follows —
`r_t = √(c²ϟ / a₀) = √(GM/a₀)` (with `ϟ = koppa = GM/c²`) — giving `v² = r_t a₀` ⟹ the BTFR
`v⁴ = GM a₀ = (c³H₀/2π)·ϟ`. *(This fixes the floor and the two asymptotes/transition radius; it does **not** fix
the interpolation shape between them — that remains [OPEN] below.)*

The *value* inherits the cosmological `H₀` seat — **an input of a different kind than α** (a boundary-state of
the network, not a coupling; §7). SDT's Hubble-tension stance (Planck = emission-dominated background, SH0ES =
local-void propagation; no expansion) makes the floor's `H₀` the *background* value → `1.042×10⁻¹⁰`, 13% from
MOND's empirical `a₀`.

**Rotation curves — numerically validated against 175 real galaxies (SPARC, 3389 points):**

| model | RMS scatter (dex) |
|---|---|
| Newtonian, baryons only (the dark-matter gap) | **0.531** |
| McGaugh empirical RAR (gold standard) | 0.196 |
| **SDT** `g_obs = √(g_bar² + g_bar·a₀)`, a₀ derived | **0.199** |

- **[GEOMETRY]** asymptotes both derived: `g_obs→g_bar` (Newton) and `→√(g_bar·a₀)` → BTFR `v⁴ = G M a₀`.
- **[OPEN]** the **crossover shape** `√(g_bar²+g_bar·a₀)` is a physically-motivated interpolation, **not yet**
  derived from the occlusion solid-angle integral. The horizon-closure mechanism above pins the floor `a₀` and
  the two asymptotes/transition radius, but **not** the combination law (how local occlusion and horizon
  relaxation add in the transition band) — that is the actual open piece.
- **[REPRODUCIBILITY — owed]** the three RMS numbers depend on the SPARC version, M/L assumptions,
  distance/inclination handling, and residual definition. They are **[NUMERICALLY VALIDATED, reproducible from
  the committed script]**, not bare **[DERIVED]** — the script + data version must ship with the claim.

---

## 4. Charge — the lattice twisting  **[CHIRALITY: GEOMETRY · CHARGE: interpretive / OPEN magnitude]**

The Boerdijk–Coxeter frustration is **locally chiral**: every hinge continuation must be left or right (the
irrational twist forbids a *local* achiral state). But the **bulk vacuum is globally balanced — `N_L = N_R`** —
otherwise it would exhibit optical activity / vacuum birefringence (unobserved). Charge is therefore a **local
handedness excess on a globally neutral medium, not a net-handed vacuum**:

- **Gravity** = the **scalar** occlusion — handedness-blind → **one sign, attraction only**
- **Charge** = the **handed** redirection of the convergent phase — focus/defocus → **two signs, ±**

```
vacuum baseline:  N_L = N_R           (neutral)
charge:           q ∝ N_R − N_L       (sign + a conservation candidate, if hinges flip only in pairs)
```

**What is actually derived** is a **two-valued chiral degree of freedom suitable for charge sign** (from the
7.356° deficit, EMC03). **What is *not* derived** is that this DOF *is* electric charge — charge additionally
requires **conservation, quantization, a Coulomb divergence law, gauge-transport invariance, the elementary
magnitude `e`, and matter-coupling rules.** None of those is closed.

- **[GEOMETRY]** frustration → two-valued chirality; gravity scalar (one sign) vs charge handed (two signs).
- **[IDENTIFICATION — interpretive]** chirality *is* charge sign; **[OPEN]** one qu-twist `= e` (EMC02).

There is no charge *substance*; "charge" is the lattice's hand. Electricity = directed transport of the twist
through the void channels — formally a **superionic conductor** (rigid framework, mobile carriers in the voids,
driven by a displacement gradient = the potential; current = twist drift).

---

## 5. Magnetism — the twist in motion  **[FORM: GEOMETRY · COEFFICIENT: SI identity / OPEN medium-derivation]**

A moving twist's occlusion field cannot update instantly (finite handoff `c`); it **lags**, and the lag is a
**transverse, circulating** component — `B`. The relativistic form is **vectorial** and forced:

```
B = (1/c²) v_twist × E_occlusion         ⟹    B ⊥ v,  B ⊥ E,  |B| = vE/c²·sinθ
∮ B·dl = μ₀ I_enc                         (Ampère)
```

This is the **relativity-origin of magnetism** (Purcell), and SDT contains the *mechanism* natively — it has
both ingredients: the **occlusion Coulomb field** (Law III) and a **finite propagation speed** (Law V).

**Why current and circulation are geometrically inseparable.** The propagation channel through the CRN voids
carries the Boerdijk–Coxeter screw geometry: the contact step decomposes `1/√10` axial to `3/√10`
circumferential — a fixed **3:1 torsion-to-translation** ratio (energy `9:1`). So an axial drift (current)
*cannot* proceed without a circumferential component (the B-twist); they are one screw motion. **This is a
geometric *mode ratio*, not a coupling** — not μ₀, not `c`; the EM magnitude still rides on α via the
constitutive identity. Keep it as the *reason* current implies circulation, never as a numerical EM constant.

**Honesty on the coefficient:** the Ampère coefficient closes "exactly" only because `1/(ε₀c²) ≡ μ₀` is the
**SI definition** `c² = 1/(μ₀ε₀)` — that is *algebraic consistency*, **not** an independent verification. SDT
still **owes the medium-derivation** of the constitutive relation `c² = 1/(μ₀ε₀)` from the hinge network.

- **[GEOMETRY]** a moving occlusion field *must* produce a transverse `B = (1/c²)v×E`.
- **[OPEN]** deriving `c² = 1/(μ₀ε₀)` (the impedance of the void) from the CRN; the absolute coupling traces to
  **α** via `k_e e² = αℏc`.

---

## 6. The unification, in one line

> **Gravity is the lattice pushing. Electricity is the lattice twisting. Magnetism is the twist in
> motion.** One corner-sharing tetrahedral medium, three motions, **one coupling α** — and the
> *compressible mode is the gravitational redshift* (RUM rotation = depth-closure).

**Black holes — two *separate* arguments, not conflated:**
1. **Opaque to the CMB** by temperature: a hole is 10⁸–10¹⁷× colder than the CMB → it absorbs it, not radiates
   it through. (A standalone thermal argument.)
2. **EHT shadows** support a photon-capturing, strong-field-opaque object at 1.3 mm — consistent with the
   opacity picture, but **EHT does not test CMB transparency** (it images near-horizon plasma emission + photon
   capture, not the CMB as a backlight). Keep the two claims apart.

SDT vs GR at the hole is the strong-field **echoes** (GOM06), not the CMB.

---

## 7. The corrected ledger

**Inputs (the whole ledger):** `α` — the one *coupling* (every local force magnitude traces to it). `H₀` — one
*boundary-state* (the network's present global relaxation rate; sets the gravitational floor only). `H₀` is
either an imported boundary condition **or** relay-derived via Law I (`Φ=Nε`, engine Book V) — in which case it
traces to `{T_CMB, ℓ_P, R_CMB}` and adds no fresh fundamental, *but* carries the FLM06 R_CMB/seed caveat.
Nothing else is tuned.

### [GEOMETRY — DERIVED, exact]
- regular tetrahedra cannot Euclidean-tile (`5·arccos(1/3) = 352.644° ≠ 360°`); deficit `7.356103°`
- icosahedral 12-around-1 shell gap `= 0.102924 r` (exact)
- face-sharing → Boerdijk–Coxeter tetrahelix, twist `arccos(−2/3) = 131.810°`, never closes
- the helix screw decomposes **3:1 torsion:translation** (`1/√10` axial : `3/√10` circumferential, energy
  `9:1`) — a *channel-mode* ratio (why current implies circulation), **not** μ₀ or `c`, **not** the substrate
- corner-sharing CRN is a known isotropic, space-occupying tetrahedral framework with rigid-unit hinge modes
- solid-angle occlusion → `1/r²`
- a moving occlusion/twist field → transverse `B = (1/c²) v × E`
- asymptotes of `g_obs = √(g_bar² + g_bar a₀)` → Newton (high-g) and BTFR `v⁴ = G M a₀` (low-g)

### [FORM DERIVED / MAGNITUDE = α (single seat)]
- occlusion force `F = (π/4) P_eff R_occ,1² R_occ,2² / r²`
- **`P_eff = m_p² m_e² c⁵ / (4π α ℏ³)`** — engine-verified (EMC04, 0.04%); **α is the only coupling**, the rest is
  whitelist → **the single-seat survives; P_eff is not a hidden second coupling**
- `a₀ = c²/C_H = cH₀/2π` — the horizon-circumference handoff rate (form derived; the `2π`=circumference, not
  radius, is the load-bearing choice); magnitude inherits the `H₀` boundary-state

### [INTERPRETIVE IDENTIFICATION — promising, NOT closed]
- the vacuum *is* a CRN (silica proves the topology occupies 3D, not that vacuum is it)
- RUM hinge-rotation *is* the gravitational redshift; `c_coord = c_∞(1−z)` as a *physical* (analog) gradient
- two-valued tetrahedral chirality *is* electric charge sign; one qu-twist *= e*

### [OPEN — correctly owed]
- **Lorentz-emergence proof** (preferred-*axis* debt dissolved; preferred-*frame* debt remains — the
  neo-Lorentzian `c_∞`; needs the relational/measurement-symmetry argument made rigorous)
- full **dispersion relation** of the random hinge network (`ω² = c²k²` with no anisotropic/dispersive residual
  to experimental limits) — required to *earn* Lorentz invariance and the transverse Maxwell polarizations
- the rotation **crossover** from the occlusion solid-angle integral (the *combination law* between local
  occlusion and horizon relaxation — the floor `a₀` and asymptotes are fixed; the interpolation shape is not)
- the **basement under the floor**: deriving `H₀` from the Law I relay without the FLM06 R_CMB/seed circularity
  (would dissolve the second input back into `{T_CMB, ℓ_P}`)
- **charge conservation + quantization**; the elementary-charge magnitude `e` (EMC02)
- the **SPARC reproducibility package** (script + data version + M/L + residual definition)
- the medium-derivation of `c² = 1/(μ₀ε₀)` from the CRN
- cosmological **d(z)** fit to Pantheon+ without Ω_Λ; the BAO **t_coupled**; the "magnetic cascade" mechanism

### [KILLED — tested, abandoned (phrased narrowly)]
- **α from numerology.** PSLQ over {π, φ, masses}: no clean closed form; `4π³+π²+π` misses α⁻¹ by **14,505σ**.
- **the *narrow* static `c ∝ (1+z)⁻¹` model** that preserves `c = fλ` while *also* requiring CMB blackbody
  cooling — ruled out by FIRAS (<50 ppm): the data forces λ-stretch + blackbody-cool ⟹ `c` fixed
  cosmologically. (Not every conceivable VSL — only this version.)
- **face-sharing tetrahedral vacuum substrate** (the BC helix as space — elongates, won't tile)
- **close-packed icosahedral substrate as a global Euclidean tiling** (frustrated)

---

## 8. Falsifiers (what kills it)

1. The quadrature crossover failing **per-galaxy** SPARC beyond the 0.11-dex intrinsic scatter.
2. A clean π/geometric expression matching α⁻¹ to **≥8 figures** (would break the "α is irreducible" pillar).
3. A detectable **preferred-frame** signal (anisotropic/dispersive `ω(k)` residual) above current limits —
   would kill the Lorentz-emergence claim that §7 leaves [OPEN].
4. Pantheon+ requiring **Ω_Λ** after the SDT static d(z) is fit honestly.
5. The CRN failing to support a derivable transverse **B = (1/c²)v×E** drag of the right magnitude once
   `c² = 1/(μ₀ε₀)` is derived from the network (not assumed).
6. The empirical `a₀` tracking the **local (SH0ES) H₀** rather than the **background (Planck) H₀** — SDT's
   horizon-closure floor uses the background value (`a₀ = 1.04×10⁻¹⁰`); a measured `a₀ ≈ 1.13×10⁻¹⁰` would
   break the "floor = background-relaxation" reading (a test MOND does not make).

---

*One coupling, one medium, three motions — with the "derived" column now split three ways so that geometry,
identification, and magnitude are never confused for one another. The question for quorum is not "is it
finished" (it is not, and §7 says so in plain type) but "are the [GEOMETRY] rows exact, the [IDENTIFICATION]
rows honestly flagged, and the [OPEN]/[KILLED] rows complete." — JCH*
