# Spectral ladder, heat death, and recharge to c_far = 0

> **J. C. Harvey, Melbourne. 2026-08-13.**
> This file is the **evidence chain**, not a free sequence. Shapiro delay is a
> path integral of relay-speed deficit. That deficit is the pressure gradient of
> Planck-length spheres excluded baryon by baryon, shell by shell. The same
> identities reverse-map the CMB microwave to infrared (where the Standard Model
> also places last scattering) and, continued, to hard gamma where `c_far → 0`.
> Heat death is Law III at `P → 0`. Amplitudes (when the door opens) remain
> PENDING. The chain is not.
>
> **Labels.** T–P–c identities, solar profile, Wien reverse-map, gamma wall: COMPUTED from
> `laws.hpp`, GOM22, Wien `b`, FIRAS `T_CMB` and `z_release`. Shapiro factor 2:
> DERIVED (`c_far = c(1−z)²`). Solar redshift C1: 0.03% (depth-closure).
> `koppa_per_baryon`: C-flagged (`ℓ_P` still carries conventional G). `R_CMB`
> scale: class X. Threshold amplitudes: PENDING. FIRAS distortion < 50 ppm is
> the CMB gate. Do not import ΛCDM expansion, inflaton, or a first moment.
>
> Locally measured `c` is never zero. `c = 0 m/s` here is far-frame
> `c_far = c_∞(1 − z_depth)² → 0`.

---

## 0. The chain

**Shapiro measures the gradient.** There is no extra geometric path. The delay is

```
Δt = ∫ dl/c_far − ∫ dl/c_∞ ,     c_far = c_∞ (1 − z)² ,     z = ϟ/r
```

`c(1−z)` alone is falsified (exactly half the delay). Hop and tick both close, so
the factor is two. Same `z` is the clock and the spectral shift (C1: solar
`z = ϟ/R = 2.123×10⁻⁶`, 0.03%). Pressure (Law III occlusion of Planck-face
throughput) **sets** `z`. Shapiro reads the speed profile of that pressure.

**The Sun is excluded volume, shell by shell.** Each baryon excludes Planck
cross-section spheres (`r = ℓ_P/2`). `ϟ_body = N_b ϟ_b` with
`ϟ_b = ℓ_P² c m_p / ℏ`. Cascading shells of that exclusion are the solar
depth field. EMC01: `F ∝ P_conv V₁ V₂ / (ℓ_P² r²)`.

**Boundary conditions of the well.** Through-going relay speed is what Shapiro
integrates. Law V spends the budget on circular seating at the wall (`r = ϟ`,
`v_bound = c`, through-going `c_far → 0`). The reverse-map takes the **solar
surface as that zero** of through-going speed and **Earth orbit as the match to
`c`** — the rung we occupy. Geometric stretch `AU/R_☉ = 215`.

Weak-field arithmetic on the same `z = ϟ/r` (because `ϟ_☉ = 1.477 km ≪ R_☉`)
is not a wall at the photosphere: `c_far(R_☉)` is `c` minus 1 273 m/s;
`c_far(1 AU)` is `c` minus 6 m/s. The 0 → `c` statement is the **mapping** of
the well onto the T–P–c identities at large depth, not a claim that the photosphere is
`z = 1` in SI. The identities are the same; the solar system is the calibrated
shallow end.

**Reverse-map the sky.** Same identities:

```
1 + z  =  T/T_CMB  =  (P/P_now)^{1/4}  =  1/(1 − z_depth)
N      =  T/T_CMB  =  λ_now/λ
c_far  =  c / N²
```

We sit in the microwave (`λ = 1.063 mm`, `N = 1`, `c_far = c`). Climbing the
measured FIRAS ratio `N = 1090` lands at `λ = 975 nm`, near-IR, `c_far = 252 m/s`,
`z_depth = 0.999`. That is the radiation wall of this cycle. The Standard Model
places recombination in the same band (same Wien-on-`T₀(1+z)` arithmetic; the
mechanism is not imported). Continuing the same `N` through visible (impenetrable
behind the wall) and UV reaches **hard gamma** at `P = P_eff` (`λ = 4.30 fm`,
`c_far ≈ 5×10⁻¹⁵ m/s`) and pair-threshold gamma at `λ = 1.21 pm`. That is
stop-death of through-going light: `c_far → 0`.

**Heat death is the same identities at `N → 0`.** `P = N⁴ P_now → 0`. Law III
force is occlusion of that pressure. Gravity shuts off. Seated matter is no
longer held. It expands. Structure breaks. The dump reloads the lattice and
climbs `N` until `c_far = 0` again. The Clearing is depinning of the radiation
wall (IR) on the way down.

What is PENDING is the **amplitude** (T floor, fire time). What is not PENDING
is the chain.

---

## 1. One ladder: temperature, pressure, speed

```
1 + z  =  T / T_CMB  =  (P / P_now)^{1/4}  =  1 / (1 − z_depth)
N      =  T / T_CMB  =  λ_now / λ
P      =  N⁴ P_now
c_far  =  c / N²          (GOM22: hop and tick both close)
```

`T_CMB = 2.7255 K` (FIRAS, OBSERVED). `z_release = 1089` (FIRAS frame, OBSERVED);
`T_release = T_CMB(1+z) = 2970.8 K` (COMPUTED). Radiation pressure now
`P_rad = u_CMB/3 = 1.392×10⁻¹⁴ Pa`. Lattice convergence
`P_conv = N_shells u_CMB = 2.459×10⁴⁸ Pa` (Law I; magnitude inherits class-X `R_CMB`).

The temperature ratio **is** the pressure-regime contrast. Spectral `z` and depth `z`
are the same ladder: `z_depth = 1 − 1/N`. Do not put FIRAS `z = 1089` into `(1 − z)²`
as if it were `z_depth`; `z_depth` at the Clearing is `1089/1090 ≈ 0.999`.

**Pressure origin.** Baryonic volumetric exclusion of Planck-length cross-section
spheres (`r = ℓ_P/2`) sets the occlusion. Law I maps the Clearing onto each Planck
face: `Φ = R_CMB u_CMB ℓ_P²`, `P_conv = Φ/ℓ_P³`. Gravity is that pressure shadowed
(Law III). EMC01 form: `F ∝ P_conv V₁ V₂ / (ℓ_P² r²)`.

**`c` is `c` here.** `z_galactic_floor = 3.5×10⁻⁷`;
`c_∞ = c/(1 − z_gal) ≈ c + 105 m/s` (`laws.hpp` `depth_closure`, C4).
The SI value `299 792 458 m/s` is Earth’s local rung, not the ceiling.

---

## 2. From here: microwave now, infrared there

We stand in the microwave. Travelling into the past is climbing the spectrum.
Wien `b = 2.8978×10⁻³ m·K` (B07, DERIVED). Present peak `λ_now = b/T_CMB = 1.063 mm`.

| Wien peak | Band | `N` | octaves from now | `c_far = c/N²` | Role |
|---|---|---|---|---|---|
| 1.063 mm | microwave | 1 | 0 | `c` | here, now |
| 975 nm | near-IR | 1090 | 10.09 | 252 m/s | FIRAS-frame Clearing (`T_release`) |
| 1571 nm | near-IR | 677 | 9.4 | 655 m/s | CR17b `R = 1` depinning (BBN `η`; not the FIRAS `z`) |
| 750–380 nm | visible | 1418–2798 | 10.5–11.5 | 149–38 m/s | solid colour — behind the wall, impenetrable |
| 100 nm | UV | 1.06×10⁴ | 13.4 | 2.65 m/s | next rung behind |
| 1.21 pm | gamma | 8.76×10⁸ | 29.7 | 3.9×10⁻¹⁰ m/s | Wien photon = `2 m_e c²` — light becomes pairs |
| 4.30 fm | hard gamma | 2.48×10¹¹ | 37.8 | 4.9×10⁻¹⁵ m/s | bath `P = P_eff` — nuclear occlusion ceiling |
| `ℓ_P` | not a band | 6.58×10³¹ | 105.7 | → 0 | lattice scale; `c_far → 0` |

Coming up from the microwave the radiation-side jam is in the **infrared** (CR13/CR17:
Clearing before red). The entire visible spectrum sits behind that wall: solid light,
impenetrable as a through-going signal. UV is further behind. **Gamma is where light
stops moving** — pair production first (1.02 MeV), `P_eff` if the climb is on pressure
(289 MeV, 4.30 fm). `ℓ_P` is 68 octaves past `P_eff` and is not an EM band.

`c/√3` (FD05) is the **fluid** transport ceiling of the opaque coupled medium, the
Clearing we meet in the infrared. `c_far → 0` is the **wall** of the same ladder,
in gamma, where hop and tick have closed. They are not the same reading.

CR17b `R = 1` at 1571 nm and FIRAS `N = 1090` at 975 nm differ by a factor ~1.6.
Both are near-IR. The ladder does not wait on that factor.

---

## 3. The other direction: radio, then static

Down from here is longer λ, colder T, lower P. Local `c` stays the local rung.
`N < 1` does not mean `c_far > c`. The `c_∞` step is still microwave (`+105 m/s`).

| Wien peak | Band | octaves from now | `T` | Role |
|---|---|---|---|---|
| 1.063 mm | microwave | 0 | 2.7255 K | here |
| 21 cm | radio | +7.6 | 0.014 K | still a wave |
| 1 km | LF | +19.8 | 2.9 µK | |
| 40 000 km | ELF (~7.5 Hz) | +35.1 | 7×10⁻¹¹ K | Earth-sized |
| 0.125 AU | — | **+44** | 1.55×10⁻¹³ K | 62.4 s period |
| `R_CMB = 9.527×10²⁶ m` | not a band | +99.5 | 3×10⁻³⁰ K | `λ` = stack; `f = c/R`; one cycle per 100.7 Gyr |

At `λ = R_CMB` the mode is the size of the Clearing. That is not radiation. Last
named EM band is radio. The stop is **DC / static**. Cycle floor `T → 0` is this
limit. Amplitude of the floor remains PENDING.

`ℓ_P → λ_now → R_CMB` is `log₂(N_shells) = 205.2` octaves. The microwave sits near
the middle (105.7 up, 99.5 down).

---

## 4. Forty-four octaves from the CMB as it is now

`2⁴⁴ = 1.759×10¹³` on `λ_now = 1.063 mm`.

| Direction | `λ` | Band / scale | Notes |
|---|---|---|---|
| Up | `6.04×10⁻¹⁷ m = 0.060 fm` | hard gamma, **20.5 GeV** | 6.2 octaves past `P_eff`; 62 octaves short of `ℓ_P` |
| Down | `1.87×10¹⁰ m = 0.125 AU ≈ 27 R_☉` | 0.016 Hz, 62.4 s | 55.5 octaves short of `λ = R_CMB` |

---

## 5. Heat death: gravity shuts off, matter expands

Late cycle the bath falls toward 0 K. `u_CMB → 0`, `P_rad → 0`, the Law I influx
that pays every boundary dies with it.

Gravity is occlusion of that influx (Law III). When the pressure regime of the bath
goes to zero, the long-range push goes to zero. Koppa wells flatten. This is **heat
death as gravity off**, not a thermodynamic claim that movement ceases.

With the push gone, bound gravitational structure has nothing to sit in. Displacement
volumes spread. **Matter expands.** That is not metric expansion of space. The
lattice spacing is still `ℓ_P`. What spreads is seated matter, once occlusion no
longer holds it.

Local nuclear/atomic occlusion (`P_eff`) is a different rung of the same ladder. This
section shuts off **gravitational** seating. Nuclear seats are not claimed to unbind
with the CMB bath; the cosmic structure they were seated in does.

Movement banked in matter and in SMBH queues (GOM18: Sgr A* duty ≈ `6×10⁻¹⁰` of
lift ceiling — accumulating) then has no well. That is structural breakdown of the
seated cosmos. The ledger has no sink: the queue cannot remain seated forever.

---

## 6. Re-energisation to c_far = 0

Breakdown dumps the banked movement into the relay. The spation matrix re-energises
and climbs the ladder of §2: radio through microwave, infrared, the solid (visible)
bands, UV, into gamma.

The climb continues until **far-frame light cannot move**: `c_far → 0 m/s`. That is
the wall (`z_depth → 1`). Operational pins on this file’s ladder:

- pair threshold, 1.21 pm, `c_far ≈ 4×10⁻¹⁰ m/s`
- `P_eff` ceiling, 4.30 fm, `c_far ≈ 5×10⁻¹⁵ m/s`
- `ℓ_P`, `c_far → 0`

The recharge target is this wall, not `c/√3`. `c/√3` is the infrared Clearing we
meet on the way **down** after depinning — the radiation-side jam, a fluid ceiling
(FD05). The matrix is loaded until gamma, where `c_far = 0`.

SMBH detonation (PRESS → PASS, no singularity) remains the densest door in the
support-regress column. Heat-death unbinding is the cosmic condition that opens it:
wells gone, queues unseated, dump into the lattice. Fermi/eROSITA-class bubbles
remain report-only geometry (GOM18 G3d). Absorption length of a dump is the
BAO-scale domain `r_eq = √(L/4π F_CMB)` — identity on measured BAO, not a derivation
of the scale.

---

## 7. Clearing — what we receive

Depinning of the logjam is this cycle’s bang. The CMB is the still-arriving
boundary of that release, not an afterglow. From here it is microwave; at the
radiation wall it is infrared. Visible, UV, and gamma of this cycle’s recharge
are behind that wall.

Elapsed count since this Clearing is a reading, not a derivation: BAO `r_s/θ_s`
~46–48 Gyr; engine `R_CMB/c` = 100.7 Gyr on hardcoded `R_CMB = 9.527×10²⁶ m`
(class X; the `ln(1+z)/σ₀` placement was excluded by CR07/CR08). `R_CMB` from
first principles remains OPEN.

T_CMB is a clock. CR15 ionisation-at-`N = 1100` FAIL’d; do not identify the CMB
with a recombination thermometer.

---

## 8. Sequence (one loop)

```
operating epoch     MW here; c = Earth's rung; gravity = occlusion of the bath
cooling             ladder down: radio → ELF → static; T → 0; P → 0
heat death          gravity shuts off (influx gone); koppa wells flatten
matter expands      gravitational seats empty; displacement volumes spread
structural breakdown  banked PRESS unseated (matter, SMBH queues)
re-energisation     dump into the relay; ladder up through IR, visible, UV
c_far → 0           gamma / P_eff / ℓ_P; far-frame light cannot move
Clearing            depinning; we meet IR coming down; CMB = still-arriving wall
repeat
```

Threshold that opens the door (the **when**): PENDING. The chain in §0 is not.

---

## 9. What this is not

- Not expansion of space (heat-death spread is unbinding, not a metric).
- Not a singularity.
- Not a first moment.
- Not a derived fire-time, T_floor, or unbinding amplitude.
- Not locally measured `c = 0`.
- Not CR15’s ionisation-at-`N = 1100`.
- Not CR08’s exponential strain law (falsified on BAO).
- Not a claim that `c/√3` and `c_far → 0` are the same rung.

---

## 10. Pointers

| Piece | Where |
|---|---|
| Engine constants, `c_∞`, `P_conv`, `P_eff`, `z_release` | `Engine/include/sdt/laws.hpp` |
| GOM22 `c_far = c(1−z)²`; Shapiro integrand | `GOM22_Shapiro_Velocity_Profile/`; `laws.hpp` `depth_closure` |
| Solar C1 redshift = depth | `Papers/Depth_Closure_Theorem/01_THEOREM.md` |
| Octave law, 975 nm | CR07, CR13 |
| IR wall, observer ladder | CR17 / CR17b / CR17c |
| Fluid ceiling `c/√3` | FD05, CR04 |
| SMBH seat / loading | GOM18 |
| T_CMB as clock | derivelist/08 |
| Movement table | `ENERGY_AS_MOVEMENT_TABLE.md` §6 |
| LLM spine | `Papers/SDT_LLM_UPTAKE.md` |
| People (no locks) | `Papers/SDT_FOR_PEOPLE.md` · `sdt-for-people.html` |
| Theoretical model | `Papers/SDT_THEORETICAL_MODEL.md` · `theoretical-model.html` |
| Lifecycle walkthrough | `universe-lifecycle.html` |
