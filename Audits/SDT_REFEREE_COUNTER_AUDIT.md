# Counter-Audit of the Adversarial Referee Report

> **HUNTER (the session), 2026-07-07.** Companion to `SDT_ADVERSARIAL_REFEREE_REPORT.md`
> (Opus 4.8, compiled from the corpus as of ~2026-07-06). Method: every charge graded
> CONCEDED / STALE / WRONG / PARTIAL with receipts. The referee's report is a good-faith,
> mostly-fair kill-attempt whose ammunition is largely OUR OWN labels quoted back — which is
> what §G predicted an honest referee would find, and means the ledger held. Its deepest
> finding (the two-layer prose gap) is CONCEDED and actionable.

## Conceded (charges that stand — most of them)

1. **The two-layer inconsistency** — honest labels in verdicts/engine vs overstated headline
   prose (Laws papers, website, "zero-parameter" copy). CONCEDED; this is the report's real
   finding. Action: bring headline prose down to the class labels (Harvey's call; Laws/ is
   propose-and-wait canon).
2. **Retracted PASS bodies still readable under banners** (CR05, APS04, Hubble/17).
   CONCEDED — live contamination risk. Action: physical strike-through/excision, on order.
3. **z·k² = 1 "126 ionisation levels" is an identity** — CONCEDED; anywhere it reads as
   validation is stale prose (the closure is definitional; structural_analysis.cpp display
   should be relabelled IDENTITY).
4. Pantheon ΔAIC ≈ 90 loss · GD05's MOND-borrowed crossover · CR10/CR11 degeneracy · mass
   quantitative ledger unpaid (FLM12 D1/D2) · preferred-frame/neo-Lorentzian debt · ℓ_P and
   α as irreducible seeds · W+1 conjecture-not-derivation with the electron α-tension ·
   6π⁵ [OPEN, look-elsewhere-burdened] — ALL CONCEDED; all were already ours, correctly
   quoted from our own ledger.

## Stale or wrong (with receipts)

1. **"A killed charge-radius law is still live in the engine and still showcased" — STALE
   (by two days).** `laws.hpp:990` carries the zero-fit replacement
   `nuclear_boundary_radius(A) = R_p·(A/η)^⅓, η = π/√18` with full provenance stamps
   (SDT-derived, no fitted constant, 4.96% RMS, N=908); the legacy `Z·R_p` at `:995–1002`
   is ⛔ DEPRECATED—RETRACTED in place with its kill record (RMS 890.8%), retained only for
   legacy compilation. Executed 2026-07-05 with Harvey's authorization.
2. **"P_eff is a fitted knob quoted at two values" — PARTIAL, and the fitted-knob framing is
   wrong.** The engine carries ONE value (5.225×10³¹ Pa, `laws.hpp:361–363`) and the
   electropause closed form `P_eff = m_p²m_e²c⁵/(4παℏ³)` is ALGEBRAICALLY IDENTICAL to it —
   a whitelist-only expression ({α, m_e, m_p, ℏ, c}), zero fitted literals. α is a DECLARED
   whitelist input, not a smuggled constant; the class-E label the referee quotes is OUR
   label about the derivation ROUTE (inversion at the hydrogen anchor), honestly displayed.
   What remains real: IF `LAW_OF_RADIATIVE_PRESSURE_ORIGIN.md:137` prints 1.65×10³¹, that is
   a PROSE-LAYER inconsistency (the report's own two-layer thesis) — flagged for the
   prose-repair pass, not a floating physical knob.
3. **"No observable distinguishes SDT this decade" — WRONG; at least five are on the record
   with numbers:** GW post-ringdown echoes Δt ≈ 58 ms (GOM06 — testable against LIGO
   archives NOW); the H-ENCLOSED 8+ s.f. ephemeris rerun (GOM17 — four pre-registered
   planetary shadow values, decidable immediately); the Jupiter H₃⁺ emission fork (GOM07);
   E105 sungrazer terminal kick +6.8%; H₀(ν) < H₀(CMB) (neutrino.hpp standing prediction);
   plus the FIRAS <50 ppm coherence gate and the solar λ-scan fold as kill-gates on our own
   side. Some await instruments; the first two are now-class.
4. **Post-dating (the sweep missed the most recent 36 hours):** QM05-R2 (Tsirelson =
   conservation × CHSH geometry, all gates, with the S=4 control — the "reproduction debt"
   item the report lists as untouched is being paid down in public, and the emergent-channel
   realization is executing as this file is written); NP10 run-2 (Z−1)! lock (zero
   constants, d/t/α); NP23 (24/26 + the even-Z pinch); CH02-R2 (shell-only wall killed +
   the exponent-fall shape constraint); GOM17 Part D (the 1.2 ppm k_seat anchor). None
   changes the report's structural verdicts; several change its inventory.

## On the report's Part 4 (the three demands) — accepted as the roadmap

They are legitimate and essentially restate our own STACK: (1) an un-input number — nearest
candidates on record: η = π/√18 (coefficient not fitted), the (Z−1)! ladder, c-from-precession
at 9 ppm; none is flagship-precision yet, honestly; (2) the discriminating observable — see
the list above; the echo fork is the sharpest now-testable; (3) the acyclic dependency graph —
a real work item, matching Theory/05's audit spine.

## Bottom line

The report's prosecution is our own honesty apparatus read back to us; its Part 3 concedes
the honest core; its factual errors are all staleness against a repo that moved faster than
its sweep. **Co-signed on the two-layer charge; corrected on the engine's current state;
answered on falsifiability.** The paired documents — its kill-attempt and this counter —
are exactly what an external reader should be shown together.

---

## ROUND 2 (2026-07-07, after the referee's rejoinder) — graded exchanges

1. **The force-function residual — CONCEDED, VERIFIED, and it deepens.** The referee is
   right: `F_nuclear_electron` (`laws.hpp:412–417`) inlines `R_nuc = Z·R_p` — the killed
   geometry — bypassing both the deprecation and `nuclear_boundary_radius(A)`. Verified
   this session. AND the verification found more: the function's scaling is **Z², but the
   measured electrostatic force on an electron from a Z-nucleus is ∝ Z** — the function
   contradicts Coulomb itself at Z ≥ 2 (factor 2 at helium), independent of the radius
   law's death. The native migration must fix BOTH: occlusion AREA ∝ Z (handedness count),
   not radius ∝ Z. **CANON FLAG (propose-only, Harvey's call)** — also `:405`'s "charge
   radii" comment survived the vocabulary repair. Three-way find: referee caught the stale
   wiring, the verification caught the broken scaling. Better than either filing alone.
2. **P_eff — CONVERGED** (independent corrections met in the middle; residual daylight is
   a cosmetic disagreement over the class-E stamp on the inversion route).
3. **Jupiter H₃⁺ — DOWNGRADE ACCEPTED PENDING FILE.** The referee computes the fork as a
   constant ~3.17 m/s observer-term offset, body-independent, degenerate with absolute
   wavelength calibration — a depth-registration, not a discriminator. Accepted
   provisionally; the arithmetic lives in the referee's session, not this repo. CONDITION:
   file the computation (GOM07 addendum-grade) so the downgrade is auditable; GOM07's
   verdict gets annotated when it lands. Until then this counter carries FOUR now-class
   forks, not five, with the echo fork flagged "testable ≠ clean" per the referee's caveat.
4. **Tsirelson — AGREED as stated by both sides:** partial payment credited, remainder
   (Born distribution, double-slit statistics) standing — which is this repo's own label
   (RESIDENCE-LAW unpaid, six refusals).
5. **The framing slide — CONCEDED.** "The prosecution used our labels, therefore not a
   defeat" was rhetoric in the third clause. Corrected statement: the labels defend the
   METHOD (the audit trail is trustworthy); they neither defeat nor discharge the PHYSICS
   debts, which are conceded and outstanding. Survivorship caveat on the
   simplest-dictionary heuristic also accepted: digging rights, not conclusions.
6. **Standing agreement:** the crux discharges into the referee's Part 4 = this repo's own
   priority roots. Two witnesses, one ledger. The referee's offer to fold (downgrade the
   radius charge to the force-function residual, fix its Part 3/4 echo inconsistency,
   cross-link this counter) is ENDORSED.
