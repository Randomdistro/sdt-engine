# Master Protocol Iteration 28 — Formal Shell-Relation Correction

**Date:** 2026-08-09  
**Target:** assess whether the attached pure-shell/stella/cuboct proposal clears
the active nuclear fail cases  
**Data class:** no new empirical data; independent formal enumeration plus
frozen references to prior observational audits  
**Canon edited:** no

## Decision

`NSEQ06_FORMAL_RELATIONS_REPRODUCED_NO_EMPIRICAL_GATE_CLEARED`

## Evidence

1. The exact cube/stella, cuboct midpoint, face-normal, and incidence counts
   reproduce in a fresh standard-library implementation.
2. The pure D/T ledger expands the engine's grouped increments algebraically;
   this is a bookkeeping clarification rather than a new prediction of the
   numbers already stored in `laws.hpp`.
3. All 20 degree-two Q-Q choices were enumerated. The explicitly selected
   `C3 × inversion` symmetry leaves two: a triangle-pair and a six-cycle. Both
   yield connected 6-regular 14-node graphs with 42 edges.
4. The 42-edge graph exceeds the simple planar bound of 36. It requires a
   non-planar/three-dimensional contact account that has not been derived.
5. The audit implementation initially failed its own source rigor: two proposal
   inputs changed, triggering a source re-freeze; then a cuboct set bug and an
   unregistered branch expectation were identified and invalidated before the
   accepted rerun. Both corrections are retained in NSEQ06 Amendments 01–02.
6. `laws.hpp` then changed after the accepted run. NSEQ06 re-froze and reran the
   current numerical ledger; it is unchanged. The new canonical prose is not
   treated as evidence beyond the independently reproduced formal counts.

## Corrections Applied

- Keep the pure-shell expansion as `FORMAL_BOOKKEEPING_CLARIFIED`, not as an
  observationally established occupancy history.
- Keep the stella/cuboct relation and the two documented graph branches as
  `FORMALLY_REPRODUCED` under their stated symmetry condition.
- Withdraw any inference that an arbitrary face-count mismatch excludes every
  face-based physical mechanism.
- Do not accept the attached transcript as evidence for its broad empirical
  claims until raw observation sources, executable reductions, and frozen
  pre-data gates are present locally.

## Non-Cleared Dependencies

- **NP12b:** an underived boundary-to-RMS charge map remains the blocker.
- **NP30:** observed radius regularities do not derive closure locations; the
  labels are imported and the claimed occupancy causal law is unadjudicated.
- **NP32:** no forward partition/cost calculation exists; N=82/Z=50 is an
  empirical anchor and P3 is false under its stated all-system gate.
- **NSEQ04:** the one-scale energy result remains calibrated and fails at BE/A.

## Next Falsifier

Before any **empirical promotion** of the canonical prose, pre-register a
graph-dynamics experiment with no inserted closure values: node state, contact
eligibility, branch-selection principle, energy/cost functional, transition
rule, predicted closure set, and a distinct observational holdout. A result
that needs a magic-number table, post-data branch choice, or fitted energy
coefficient is not a native prediction and must retain the corresponding
unresolved status.
