# Public Paper Canon Ledger

Frozen for the 2026-08-18 corpus rewrite. This register constrains every
`paper-*.html` route. Numerical authority remains
`Engine/include/sdt/laws.hpp`; this file does not supersede the engine or the
Ruleset.

## Input and ontology distinctions

- The four ontological categories are Space, Matter, Movement, and the
  Ever-Present Now. They are not four numerical inputs.
- `ℓ_P` is the lattice pitch (Axiom R1). Relations that recover it through a
  quantity already containing `ℓ_P` are identities.
- `c`, `k_B`, and `e` are unit bridges. `{ℏ, m_e, m_p}` occupy one
  action/mass seat. `α` is the hydrogen rung. `a₀` is an atomic seat.
  `T_CMB` is the present boundary clock.
- The 10.10-octave spectroscopic endpoint and the 10.47-octave
  red-to-infrared interval have different definitions and may not be
  interchanged.

## Force and interaction provenance

- The Law-III geometry
  `F = (π/4) P_eff R₁²R₂²/r²` is distinct from the magnitude of `P_eff`.
- The engine magnitude is `CALIBRATED(1)` at the hydrogen interaction anchor.
  The electropause closed form is a derivation-basis correspondence with `α`
  retained. Their coexistence is an open provenance question.
- The `6π⁵` tube-volume relation is shared-input, not an independent
  prediction.
- Nuclear boundary scaling is `√Z` where area is proportional to `Z`; the
  superseded linear-radius form is withdrawn.

## Gravitation and cosmology

- `ϟ = v²R/c²`, `z = ϟ/r`, and `v²r = c²ϟ` are the active celestial
  relations. No gravitational constant or weighed source mass is fundamental.
- Mercury reconstruction of `c` is a consistency route because its surface
  speed is orbitally inferred. It is not an independent derivation of `c`.
- The repaired Shapiro relation retains the factor required by the direct
  assessment.
- Failed galaxy and cosmology routes remain visible as excluded. The native
  whole-range galaxy profile, luminosity mechanism, cosmological
  distance-redshift law, and cyclic amplitude remain open.

## Atomic and nuclear

- The Lamb extraction is withdrawn. Measured Lamb intervals remain comparator
  data.
- The constitutional stable-nucleus grammar for `Z ≥ 2` is one alpha core plus
  `n_d = 3Z - A - 2` deuterons and `n_t = A - 2Z` tritons; electron-capture
  nuclides use the recorded alternate grammar.
- The current construction language is tier/belt grammar. Superseded
  dual-tetrahedron and frustration-selector mechanisms are not active.
- `koppa_per_baryon` contains `ℓ_P` through its definition. Baryon counts built
  from it carry that circularity risk and are consistency routes.

## Evidence semantics

- `DERIVED`, `COMPUTED`, `CALIBRATED(n)`, `OBSERVED`, `PENDING`, `IDENTITY`,
  `CONJECTURE`, `OPEN`, `EXCLUDED`, and `WITHDRAWN` retain their gateway
  meanings.
- Algebraic identity, shared input, and fitted-anchor agreement do not count as
  independent evidence.
- Papers 12, 14, 15, and 16 are predominantly specifications. Their equations
  define test programmes unless a direct assessment supports a stronger label.
