# Corrected Route — Electron Orbit Tube to Proton Torus

> **Author:** J. C. Harvey, Melbourne. **Recorded:** 2026-08-14.  
> **Status:** RESOLVED CONSTRUCTION — shared-input, not an independent
> prediction; supersedes the withdrawn PPT17 phase-count interpretation.

## Construction

The mass ratio is a displaced-volume packing statement, not a count inferred
from the proton's `1.83c` surface speed.

Let:

- `a₀` be the hydrogen ground-state orbit radius;
- `b_e(a₀)` be the physical radius of the point electron's swept tube at that
  orbit (if “width” is supplied as a diameter, `b_e=width/2`);
- `L_e` be the already-defined electron configuration-closure path length;
- `N_pe` be the proton/electron displaced-volume count;
- `R_p=0.8414 fm` be the major seat radius of the proton torus;
- `b_p` be the proton tube radius.

The electron's swept volume is

`V_e = π b_e(a₀)² L_e`.

For one geometric orbit, `L_e=2πa₀`. If the physical volume ledger uses the
electron's full 720-degree orientation closure, `L_e=4πa₀`. This choice must be
inherited from the electron construction, not selected from the mass ratio.

Pack the electron tube volume into the proton form:

`V_p = N_pe V_e`.

For a torus envelope seated at `R_p`,

`V_p = 2π² R_p b_p²`,

so

`b_p = b_e(a₀) √[N_pe L_e/(2πR_p)]`.

For `L_e=2πa₀`, this reduces to

`b_p/b_e = √(N_pe a₀/R_p)`.

The trefoil refinement must use its actual finite-tube centreline length:

`V_p = π b_p² L_(2,3)(R_p, aspect)`,

with overlap counted once. Conservation of tube volume, not an independently
invented phase-space measure, determines the resulting width.

## Point-electron occlusion dynamics

The electron is the point test body following the central occlusion profile.
At the Bohr seat,

`r=a₀`, `v=αc`.

The native occlusion/koppa length is therefore

`ϟ_H = v²r/c² = α²a₀ = r_e`.

The point-particle orbital profile is

`v(r)=c√(ϟ_H/r)=c√(r_e/r)`.

Following that same profile inward to the proton seat gives

`v(R_p)/c = √(r_e/R_p) = 1.830057...`.

Thus `1.83c` is a consequence of the point-electron occlusion structure at the
`0.8414 fm` seat. It is not the source of the integer five in `6π⁵`.

## Certification

The physical closure is the full 720-degree path, `L_e=4πa₀`. Equating
`6π⁵ V_e` with the ring-torus volume gives

`b_p/b_e = √(2·6π⁵·a₀/R_p) = 15197.2363`.

For the engine's provisional point-body radius `b_e=3×10⁻²⁰ m`, this gives
`b_p=0.455917 fm`, below `R_p=0.8414 fm`; the result is a valid ring torus.
The absolute width remains provisional, but it cancels from
`V_p/V_e=6π⁵`, so it does not reopen the ratio construction.

No phase-lock rounding and no new exponent assignment enter this route.

