# FD03 — Direct re-run verdict (2026-07-26)

> **Run class: DIRECT** (main session, no agents). Agent-era `FD03_VERDICT.md`,
> `FD03_DERIVATION.md`, `fd03_results.txt` deleted 2026-07-26 (git-recoverable).
> Tool: `fd03_reynolds_relay_ratio.cpp`, g++ 15.2 -O2, exit **0**.
> Output: `fd03_rerun_2026-07-26.txt`.

## Prompt contraband check

**CLEAN — and the prompt names its own trap** (§④ P2 anti-tautology gate: ℓ_c must be computed
forward from lattice geometry; a back-solve from 2300 is CALIBRATED(1), cap at C, "the prompt
forbids reporting it as DERIVED"). R0–R5 inherited; whitelist-only.

## What the direct run measured

- **P1 (derived, real):** Re = τ_relay/τ_adv = UL/ν as a like-rate ratio of one medium —
  identity exact (1.1e-16); the dimensionlessness of Re is *explained*, not assumed.
- **P2:** Re_crit = (L/ℓ_c)² derived as the coherence crossing (ν cancels — the threshold is
  pure geometry). ℓ_c/L = 1/48 **back-solved from measured 2300 → CALIBRATED(1)**, exactly the
  C-path the prompt pre-declared.
- **P3:** plate needs a **second** calibration (L/ℓ_c = 3536); direction (open ≫ confined) is
  right. Taylor–Couette: the pipe-class confined anchor lands O(2×10³) vs measured 1708 — a
  transfer within factor ~1.3, the one cross-geometry statement not separately fitted.
- **P4:** lattice crossover vs analytic single-mode model: 8.3e-05 relative — internal
  consistency of the mechanism, model-vs-itself.

## Correction to the tool's scoreboard

**F2 ("pipe threshold in band 2000–4000: YES") is struck as a test.** ℓ_c was set FROM 2300;
once anchored, F2 cannot fail — an unfailable gate is ceremony, not falsification (empty-class
rule). The scoreboard's honest content is F1 (identity), F3 (a finite crossing exists), F4's
ordering direction, F5 (internal consistency), and the un-fitted Couette transfer.

## Verdict

- **Prompt completion: B** (all four phases run; C-path taken and labelled; scoreboard corrected
  here).
- **Physics class: CONVERGENCE (Class C, CALIBRATED(1) per geometry).** Banked: the
  dimensionlessness derivation (Re as two competing rates of one medium — a first-principles
  account the textbooks don't give), the coherence-crossing mechanism with ν cancelling, the
  open-vs-confined ordering, and the pipe→Couette same-anchor transfer (~1.3×). NOT banked:
  Re_crit = 2300 (calibrated in, cannot count out). **The A-path prize stands open: forward-compute
  ℓ_c from the 2D neighbour count and the least-damped confined mode — a number that exists
  before any measured Re is read.**
- **Recovery:** n/a · **Cascade root:** forward ℓ_c (shares the lattice-geometry closure with
  FD01/FD02's ν coefficient).
