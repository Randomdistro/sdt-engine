# FD04 — Direct re-run verdict (2026-07-26)

> **Run class: DIRECT** (main session, no agents). Agent-era `FD04_VERDICT.md`,
> `FD04_DERIVATION.md`, `fd04_results.txt` deleted 2026-07-26 (git-recoverable).
> Tool: `fd04_kolmogorov_cascade.cpp`, g++ 15.2 -O2, exit **0**.
> Output: `fd04_rerun_2026-07-26.txt`.

## Prompt contraband check

**CLEAN.** derivation basis stated in full; K41 named as the thing to out-derive, not import; C_K and
intermittency pre-declared as honestly-OPEN. R0–R5 inherited.

## What the direct run measured

- **P1:** shell ledger with imposed constant flux → u_ℓ ∝ ℓ^(1/3), exponent 0.333333 ± 9.5e-17,
  ε_n spread 5.6e-16. PASS.
- **P2 (core):** E(k) = C_K ε^a k^b solved as a 2×2 dimensional balance → a = 2/3, b = **−5/3**
  exactly; numerical fit on the synthetic spectrum −1.666825 ± 6e-4. PASS.
- **P3:** η = (ν³/ε)^(1/4) finite; L/η ∝ Re^0.750000; deep limit with ν_lattice = ℓ_P·c bottoms
  at η = ℓ_P exactly — floor respected. PASS.
- **P4:** slope vs the cited tidal-channel inertial range (−5/3 over ~3 decades): match. PASS.
- **P5:** C_K held OPEN (not fitted to 1.5); intermittency held OPEN. Honest.

## Corrections this verdict applies

1. **The exponent's class is CONVERGENCE, not A-native-win.** The 2×2 closure (constant flux +
   dimensional balance) is the same algebra K41 runs; shared closure cannot discriminate. SDT's
   side has its own origin — volume-conserved displacement handoff, the same shell law the canon
   already carries (u ∝ 1/r²) — so this is convergence with independent origin, banked as
   coherence, not as a win over Kolmogorov.
2. **Intermittency is a LIVE TENSION, not a neutral OPEN.** As posed, the volume-conserved
   cascade predicts NO anomalous correction at leading order; measured spectra run ~ −1.71
   (−5/3 − 0.04). Either SDT derives the correction from lattice mechanics (nested-knot
   intermittency?) or the deviation stands as a recorded miss against the as-posed cascade. The
   tool catalogues it OPEN; this verdict names it the one place FD04 can actually lose.
3. The ℓ_P floor (F4) is safe by ~30 orders of magnitude in every real flow — a consistency
   statement of the ontology, not a testable prediction. Recorded as such.

## Verdict

- **Prompt completion: A** (five phases, gates as committed, opens held open).
- **Physics class: CONVERGENCE (Class C)** — exponent and microscale scalings reproduced from a
  native-origin flux ledger; C_K OPEN; intermittency = live tension owed a derivation or a
  recorded miss.
- **Recovery:** n/a · **Cascade root:** C_K from a lattice shell count; intermittency correction.


## Phase-6 direct result

> **Run class: DIRECT** (main session, no agents). P6 pre-commit written into PROMPT.md before
> the tool; tool `fd04_p6_intermittency_repose.cpp`, g++ 15.2 -O2, exit 0.
> Output: `fd04_p6_rerun_2026-07-26.txt`. Mechanism: Harvey's two dictated sentences (the wrap;
> the bath's refusal), 2026-07-26.

## The chain (nothing fitted)

Aspect **R = 4r** from the 1:4 gearing (the wrap) → generation scale-step **λ = 4**. Leak per
generation **g = 0.103** — the lattice's own frustration gap; the tool re-derives its geometric
identity from scratch: the icosahedral 12-around-1 edge gap is 0.05146, and the canon's 0.103
is exactly **2× the edge gap (0.1029)** to four figures. β-model closure → 3−D = 0.0784.

## Results against the committed gates

| item | value | standing |
|---|---|---|
| μ_pred (slope correction) | **0.02614** | **G6 PASS** — inside the committed band [0.01, 0.10] |
| predicted slope | **−1.6928** | measured −1.70/−1.71; **1.53× under the measured centre — reported, not absorbed** |
| rule (a) dead branch | slope −2.058 | printed dead, as pre-computed in the reopen docket |
| sensitivity spread | −1.679 … −1.693 | all conventions in band; primary is the committed one |
| ζ_2 (secondary) | 0.693 | measured ~0.70–0.71 — close |
| ζ_4 (secondary) | 1.307 | measured ~1.28 — close |
| ζ_6 (secondary) | 1.922 | measured ~1.78 — **single-leak shortfall, printed as committed** |

## Verdict

- **Prompt completion (P6): A** — chain as committed, gate as committed, dead branch and the
  distance from the measured centre both printed.
- **Physics class: CONVERGENCE-candidate (Class C), first landing.** This is the FD suite's
  first intermittency number carried by geometry alone: aspect from the wrap, leak from the
  lattice, step from the wrap — against a rival account that fits an entire multifractal
  spectrum to the same data. It is NOT yet the discriminator: the prediction sits 1.5× under
  the measured central anomaly, and the single-leak model under-corrects p ≥ 6 exactly as
  pre-declared. The named refinement — the refusal COMPOUNDS if the frustrated fraction applies
  to routes rather than volume, steepening high orders — gets its own pre-commit before any
  number is touched.
- **What the bath sentence bought:** the handoff rule that closed the pre-commit — each
  generation re-negotiates with the same scale-free medium, which allows only the freedoms
  oppositional drag can instil; intermittency is the medium's refusal fraction. "Nothing
  happens by itself" is now a computed number.
- **Recovery:** compounding-refusal refinement (own pre-commit) · **Cascade root:** none new —
  the leak is the same 0.103 the lattice fork already carries.
