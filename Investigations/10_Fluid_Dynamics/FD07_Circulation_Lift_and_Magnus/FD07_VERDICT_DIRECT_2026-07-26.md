# FD07 — Direct re-run verdict (2026-07-26)

> **Run class: DIRECT** (main session, no agents). Agent-era `FD07_VERDICT.md`,
> `FD07_DERIVATION.md`, `fd07_results.txt` deleted 2026-07-26 (git-recoverable).
> Tool: `fd07_circulation_lift_magnus.cpp`, g++ 15.2 -O2, exit **0**.
> Output: `fd07_rerun_2026-07-26.txt`.

## Prompt contraband check

**CLEAN — the prompt names its own trap** (⓪.3: if the 2π is supplied by writing Γ = πUcα, that
is an IMPORTED coefficient, cap at C). R0–R5 inherited.

## What the direct run measured

- **P1 (native):** L = ρUΓ from occlusion asymmetry; lift sign toward the fast face. PASS.
- **P2:** dC_L/dα = 2π to 1.4e-14 — with the tool's own provenance flag: **the 2π is
  potential-flow geometry, BORROWED**; SDT supplies mechanism (ℓ=2 traction threads Γ), not the
  coefficient. Exactly the C-cap the prompt pre-declared.
- **P3a (native, the good one):** Kutta condition from Law-V continuity — the un-pinned corner
  flow would hit v = c at s* ~ 1e-14 m, so the lattice must pin the rear stagnation point. An
  *imposed-by-fiat* rival axiom dissolved into the relay ceiling. PASS.
- **P3b:** Magnus sign correct; ideal C_L = 2πS vs measured O(1) — right order, viscous deficit
  honestly pointed at FD06.

## Corrections this verdict applies

1. **C4 (Γ = Nκ, N ≫ 1) is struck as a test.** Any macroscopic circulation divided by κ = h/m
   gives N ~ 10⁸ — the gate cannot fail (empty-class rule). It stands as a consistency narration
   linking FD02, nothing more.
2. **The genuinely-falsifiable test the prompt ordered was NOT run:** derive the lift-curve slope
   from the ℓ=2 channel solid-angle geometry *without* pre-plugging 2π, and see whether it lands
   at 2π or materially away. That test — the one place FD07 could actually win or lose — remains
   **OPEN**, untouched by the tool.

## Verdict

- **Prompt completion: C** (P2's A-path unexecuted; C4 struck).
- **Physics class: CONVERGENCE (Class C).** Banked native: lift sign, Magnus sign, and Kutta as
  Law-V relay continuity (a rival fiat dissolved into mechanism). Borrowed and labelled: the 2π.
  The open prize is the ℓ=2 solid-angle slope derivation — it has a populated failure class and
  should be the next tool built here.
- **Recovery:** n/a · **Cascade root:** ℓ=2 wake solid-angle geometry (shares PPT06/APS04
  wake-multipole closure).
