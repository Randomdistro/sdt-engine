# SAR01 — VERDICT: Le Sage Thermal Budget (§K kill-deepen re-run)

> **Run class: DIRECT** (main session, no agents), 2026-07-26. Unanchored per §K.4 — the ceiling
> below was committed from the geothermal budget alone, without reading the deleted prior result.
> **Author:** J. C. Harvey, Melbourne (SPEC) · executed direct. Tool: `sar01_lesage_budget.py` → `results.txt`.
> Native throughout — gravitational force as `M v²/r`, no G, no GM.

## Dual verdict

| Axis | Grade | One-line |
|------|-------|----------|
| **Prompt completion** | **A** | All three routes run; η ceiling re-derived clean; recharge honestly OPEN. |
| **Physics class** | **KILL-OVERTURNED / NATIVE** | Occlusion is a pressure shadow (throughpole), needs no terminus — Le Sage's death does not transfer to SDT. |

## The §H trichotomy — where the flux goes

- **THROUGHPOLE** — the convergence flux passes through Earth (in one side, out the other). SDT's
  gravitational push is the **pressure shadow** the occlusion casts, *not* captured or destroyed flux.
  So the net push exists **without** any flux being removed. **PASS, no heat.**
- **BUFFER** — none required for the push.
- **TERMINUS** (forbidden) — Le Sage's corpuscular push *requires* one: corpuscles must be absorbed to
  produce net force, and that absorbed momentum thermalises → the classic melt. **SDT needs no terminus**,
  because the shadow (a pressure asymmetry) does the pushing, not a captured flux. This is the whole
  difference, and it is why the Le Sage kill does not apply here.

## Route A — the leakage ceiling (re-derived clean, native)

The forbidden fraction η is the part of the redirected throughput that leaks from throughpole/buffer
into terminus-like heat. Its ceiling is the geothermal budget over the redirected throughput:

```
F_grav = M_E · v_orb² / r_orb        = 3.541×10²²  N        (native, no GM)
P_occ  = F_grav · c                  = 1.062×10³¹  W        (redirected throughput)
η_max  = P_geo(47 TW) / P_occ        = 4.43×10⁻¹⁸           [CEILING]
```

Reproduces the prior earned 4.4×10⁻¹⁸ from the budget alone. SDT's elastic redirection is
structurally **η ≈ 0**, ~18 orders under the ceiling — a vast margin.

**Guard honoured:** `f` (the EMC01 transfer/pressure ratio) was **not** multiplied in as a heat
efficiency. The killed formula `P_heat = f·P_conv·V_disp·N` (which treated a pressure ratio as an
energy-coupling efficiency) is **not** resurrected.

## Route B — recharge / the Maxwell objection (OPEN, honest)

What restores the isotropy of the redirected flux after occlusion is **not fabricated here**. For a
throughpole it is maintained by the relay structure, but the *microphysics* of that restoration is the
**Law-II release-rate law** — flagged **OPEN** at that root, not invented. (Same root several branches owe.)

## Route C — the ledger closes

In → through → out closes as **throughpole + (no buffer needed)**, with the terminus fraction bounded
by η_max. No step requires destruction-with-no-efflux to make the push. Le Sage dies on exactly that
step; SDT does not reach it.

## §K.5 outcome

> **KILL-OVERTURNED.** The SAR01 heating "kill" rested on the absorption premise (a transcription
> error): occlusion redirects, it does not capture. With the premise fixed, the push is terminus-free
> and the heating ceiling (η<4.4×10⁻¹⁸) is met by ~18 orders. The one honest open is the recharge
> microphysics (Law-II release-rate root), which is **named, not faked**.

## Verdict line

> **Prompt A · Physics KILL-OVERTURNED/NATIVE · η ceiling 4.43×10⁻¹⁸ (re-derived clean, no GM) ·
> occlusion terminus-free (pressure shadow = throughpole) · recharge OPEN (Law-II root).**
> Le Sage's thermal death does **not** transfer to SDT.

*SAR01 · direct 2026-07-26 · absorption→redirection premise enforced; f kept as a pressure ratio;
no fabricated recharge.*
