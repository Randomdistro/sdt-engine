# SAR05 — VERDICT: Provenance After Iron

> **Run class: DIRECT** (main session, no agents), 2026-07-26. Replaces the prior "automated
> execution" verdict — same tool, re-run and re-checked by hand; the agent-provenance is removed but
> the result stands because it reproduces. **SPEC:** J. C. Harvey, Melbourne. Tool: `sar05_provenance.py`
> → `sar05_results.txt`; ledger `SAR05_PROVENANCE_LEDGER.md`.
>
> *Note on the re-run: this was honest automated execution, not a fabrication — every checkable number
> reproduces, and the one place it could have over-claimed (the after-iron fork) it correctly declared
> DEGENERATE. Re-issued DIRECT for provenance, not because anything was wrong.*

## Dual verdict

| Axis | Grade | One-line |
|------|-------|----------|
| **Prompt completion** | **A** | All four phases run and gated; ledger + results written; direct re-run reproduces. |
| **Physics class** | **CONVERGENCE (ledger + seeding)** · **fork DEGENERATE (single event) / LIVE (population)** | An audited reframe, not a new number; the fork is honestly un-winnable per event. |

## Phases (direct re-run)

- **P1 — provenance ledger.** Every "fed from beyond the CMB" reach resolves to a **local, finite,
  formation-time battery** (fission / spallation / photodisintegration / r-process / accretion /
  directed-pressure). 0 rows require beyond-CMB **at decay**. The decay-time vs formation-time boundary
  is the cut; a formation-time charge must not masquerade as a decay-time feed (FLM11 caution). **PASS.**

- **P2 — the after-iron fork (self-consuming vs pressure-fed).**
  - **Single event: DEGENERATE.** The "pressure battery" and the "internal budget" are the *same*
    gravitational binding energy read through two cuts (NP19) — ~3×10⁴⁶ J vented in ~seconds either way,
    observationally identical. Declared, not faked.
  - **Population: LIVE.** The committed separator `sign(dL_ν/dṀ)` splits them — flat (self-consuming)
    vs rising (pressure-fed) at fixed core mass. NATIVE-eligible only at population level, reported as such.

- **P3 — z≈99 → 273 K (verified independently, not just from the tool).**
  T_CMB·(1+z) at z=99 = 2.72548×100 = **272.5 K**; inverse, z_at_T(273.15) = **99.22**. So the CMB
  backtrack hits the **freezing point of water at z≈99** — clean round-trip. This is the **only**
  surviving beyond-CMB question, and it is *cosmological provenance* (what set the gradient at z≈99),
  cleanly separated from any decay-time feed. **PASS.**

- **P4 — the 2/5 seeding.** Y_e = 2/5 = 0.4 (2 protons per 5 baryons). At Y_e = 0.4 the r-process makes
  the **first peak (A≈80), not gold** — gold needs Y_e ≈ 0.1 (far more neutron-rich). The climb to gold
  is **n,γ-dominated with occasional β⁻**; the earlier "β⁻ on every rung" overcount is **withdrawn**.
  Correct nucleosynthesis; the 2/5 is an integer ratio, not fitted. **PASS.**

## Verdict line

> **Prompt A · CONVERGENCE (ledger/seeding) · fork DEGENERATE per-event, LIVE per-population ·
> z≈99→273 K verified · 2/5 exact.** "Beyond the CMB" stops being a free reservoir invoked on demand:
> everywhere in the forge and the decay it resolves to a local finite battery, and it survives **only**
> as one well-posed cosmological question (the z≈99 gradient), distinct from refuel-at-decay.
> Provenance closed for the sibling set.

*SAR05 · direct re-run 2026-07-26 · agent-provenance removed, result reproduced and re-checked; the
one honest degeneracy (single-event fork) is stated, not dressed as a win.*
