# HUNTER SCOUR 2026-07-02 — Nuclear Physics region NP01–NP11

> Auditor: HUNTER at maximum pressure, assume-fabrication-until-disproven. Region:
> `Investigations/05_Nuclear_Physics/` NP01–NP11 (+ NP17 as the load-bearing cross-reference,
> +NP04A/B). Protocol: `HUNTER_PROTOCOL.md`, model on `APS04_HUNTER_AUDIT.md`.
> **All C++ tools recompiled with MSVC (BuildTools 2022, `cl /std:c++20 /EHsc /O2 /utf-8`) and
> RUN; NP05 Python re-run. Every committed output was reproduced byte-for-byte (modulo em-dash
> console encoding). Nothing in this region is a non-compiling "validated" tool.** Scratchpad
> builds only; NO audited file edited; `Engine/` and `Laws/` untouched.

## Headline

**The NP nuclear region is clean.** In sharp contrast to APS04 (fabricated PASS, non-compiling
tool), NP01–NP11 contains **no fabricated result**. The famous measured targets are handled
**honestly**: the deuteron/alpha binding tools *report their misses as misses* and the neutron
lifetime is *explicitly labeled un-derived (class F)*. The only genuine faults are (1) one
INFLATED cross-reference wording in the NP09 verdict, and (2) a handful of MINOR
disclosed-but-trivial "PASS" labels and a self-flagged numerology. No BROKEN, no CIRCULAR-hidden,
no BORROW-SMUGGLE. Most of NP07/08/10/11/03/06 are **unexecuted SPEC prompts** — nothing claimed,
nothing to fabricate.

## Fault table

| # | Investigation · claim | Fault type | Evidence | Severity |
|---|---|---|---|---|
| 1 | **NP09 verdict / tool stdout**: "NP17 shared-electron route gives **2.224 MeV exactly** (factor 3 geometry)" (`NP09_VERDICT.md:20`, `np09_deuteron_binding.cpp:62`) | **INFLATED** (over-claimed cross-ref) | NP17's own recompiled output is **2.2004 MeV, −1.06%** — NOT exact. Only the *factor 3* (−4+1) is exact; the *energy* is 1.06% off. "exactly" misstates a sibling's disclosed near-miss. | MINOR |
| 2 | **NP02 Stage B**: `E_occ_unit(He-4) = 28.30 MeV = B_meas 28.30` | IDENTITY-PASS **(disclosed)** | `k_occ` is alpha-pinned so He-4 lands on itself by construction. Tool states it: "E_occ_unit … alpha-pinned (CLASS E; not trusted for ranking)… MeV column is a calibrated proxy." Real claim = parameter-free *ranking* of `B_geom/A`. Not hidden. | MINOR |
| 3 | **NP02 Stage E**: "predicted mirror shell … 20/20 PASS" | Near-tautology labeled PASS **(disclosed)** | Prediction is `Z mod 8` column, and `Z = 2+n_d+n_t` is itself a grammar output → the "prediction" re-uses its input. Tool discloses: "confirms the COUNT mirror, not yet the GEOMETRY mirror … mirror is partial." | MINOR |
| 4 | **NP02B #13**: `Z³(In-111)/Z³(Be-7) = 1838 ≈ m_p/m_e` | NUMEROLOGY **(self-flagged)** | Post-hoc ratio of two cubed Z's landing near a famous constant. `CLASSIFICATION.md` row 13 flags it: "C-flagged *(numerical coincidence; risk_flag: post-hoc ratio)*." Honest, but the "≈ m_p/m_e" match is highlighted in-run. | MINOR |
| 5 | **NP04A Stage C**: "predicted topological branching 1.13% [P4, **class D novel-prediction**]" | INFLATED label | The "1.13%" is literally `(τ_beam−τ_bottle)/τ_bottle = (887.7−877.75)/877.75` — an arithmetic restatement of the input beam/bottle gap, not a prediction. τ's are hard-coded (`np04a:63`), not derived. Tool *does* immediately hedge "barrier INFERRED from τ, not yet predicted → class F". Label "class D novel-prediction" over-reaches the hedge. | MINOR |
| 6 | **NP09 `results.txt` vs `NP09_VERDICT.md`** P_eff label | Internal label drift (NOT a fault vs canon) | `results.txt:10` calls P_eff "[CALIBRATED from H]"; verdict calls it "class E coefficient." Both consistent with `laws.hpp:361` (`calibrated-target`) and `:401` (class E). No fabrication — a wording drift only; flagged for completeness. | MINOR |

**No faults graded FABRICATED / BROKEN / CIRCULAR-hidden / BORROW-SMUGGLE were found in NP01–NP11.**

## Honest PENDING / KILL / negative results — verified genuine (NOT faults)

These are the corpus's honest floors; the protocol forbids calling them fabrications, and they check out:

- **NP09 (deuteron, occlusion route) — honest FAIL.** Recompiled: `E_bind = 0.0600 MeV` vs 2.224 (**−97.3%**),
  VERDICT: FAIL, exit 0. Reverse-engineering is shown openly (R_n,eff = 5.12 fm = 6.09× R_p, flagged unphysical).
  The tool does NOT dress the miss as a PASS. Class E/PARTIAL, self-labeled. This is the correct handling of the
  famous target the session's FLM12-D1 also could only bracket. **NP09 does not over-claim its own number** — its
  only sin is the *inflated citation of NP17* (fault #1).
- **NP17 (deuteron, shared-electron route) — honest near-PASS, PREDICT not bracket.** Recompiled: `E = 3αℏc/D`,
  `D = 2R_p+R_p/3 = 1.9633 fm` → **2.2004 MeV (−1.06%)**, single asserted geometry (no scan → not a bracket).
  Energy scale is `αℏc` (the engine's class-F Coulomb identity), no fitted nuclear constant. The `R_p/3` node gap
  is asserted ("trefoil minor radius"); the true gap for 2.224 is 0.260 fm vs R_p/3 = 0.2805 — the 1.06% is that
  node-radius slack, disclosed as "the 1% is the node-radius precision, not a tuned energy." Alpha honestly
  PENDING (4.45 vs 28.3 MeV, under-binds; the "0.265 fm < 2R_p → unphysical" is shown, not hidden).
- **NP04A/B — honest ledger.** Recompiled, output byte-identical to committed run_logs. Tritium keystone
  `Q_β(³H) = Q_n − [B(t)−B(³He)] = 18.3 keV` (meas 18.591) is **parameter-free arithmetic**, and NP04A *itself*
  warns of the "you rediscovered nuclear mass accounting and called it SDT" reading, splitting H1 into arithmetic
  (class C) vs mechanism (C-flagged). Mass ratio downgraded to **F** ("D2 lever … NOT C until the lever is
  derived"). No home-team pass.
- **NP11 neutron lifetime — NOT derived, honestly.** There is **no NP11 tool** (PROMPT-only). The only τ_n
  treatment lives in NP04A Stage C: `ln(f·τ) = 60.6`, "barrier **INFERRED from τ, not yet predicted → class
  F/PARTIAL**." τ is *not* fitted-to-880 and then re-predicted — it is openly declared un-derived. Correct.
- **NP05 (Gate G1 price of displacement) — clean KILL, verified.** Python re-run reproduces the union volumes
  (`R7 dV = −67.224` matches the "67 fm³" narrative). The linear `E = price × geometric-deficit` functional is
  **FALSIFIED** at G1 (spread 45×, bar ≤1.35) and G4 (σ<0 unphysical; blind preds +158…+1099%). Thresholds were
  committed *before* the run. **Self-caught a 10¹⁵ unit slip** in the protocol's own Machian claim (grepped repo,
  "15.35 MeV/fm³" found nowhere = P_conv with 15 orders dropped). Refused to tune `s` per-reaction (R5). Successor
  (`E = ℏΔω`) named with a declared acceptance test. Textbook honest-negative.
- **NP01 (grammar), NP02/NP02B (growth)** — recompiled, outputs byte-identical to committed. NP01 is a pure
  integer (Z,A)→(n_d,n_t) transform with 7 compile-time `static_assert` targets (287 records, Σn_d=7658, etc.) —
  no physics claim to fabricate. NP02/02B self-classify the one fitted scale (`k_occ`, class E) and disclose the
  inversion "n_t == unpaired e⁻" as **only 48% true** — "disclosed, not laundered."

## Per-NP status line

- **NP01** — Atomicus grammar. Recompiled, byte-identical, 7 static_asserts pass. **CLEAN** (pure integer transform, no claim to fabricate).
- **NP02** — Stellar fusion/growth. Recompiled, byte-identical. **CLEAN**; one disclosed IDENTITY-PASS (He-4=28.30 by k_occ pin) + one near-tautological 20/20 mirror, both self-flagged (faults #2,#3, MINOR).
- **NP02B** — Grammar chain growth. Recompiled, faithful. **CLEAN**; one self-flagged numerology (Z³ ratio ≈ 1838, fault #4, MINOR).
- **NP03** — Mirror falsification (+A–D). **SPEC ONLY** (5 PROMPT.md, no tool/output/verdict). Nothing claimed.
- **NP04A** — Neutron decay ledger + tritium keystone. Recompiled, byte-identical. **HONEST PASS** (keystone parameter-free); one inflated "class D" branching label (fault #5, MINOR); mass ratio honestly F.
- **NP04B** — 3n genesis gate. Recompiled, byte-identical. **HONEST** (energy gate PASS; kinetics openly UNRESOLVED/F).
- **NP05** — Gate G1 price of displacement. Python re-run reproduces deficits. **CLEAN KILL** (falsified G1+G4, self-caught 10¹⁵ slip, successor named). Exemplary.
- **NP06** — Gear-frequency binding functional. **SPEC ONLY** (PROMPT.md). NP05's named successor, unbuilt.
- **NP07** — Primordial lithium. **SPEC ONLY** (PROMPT.md). Famous Li problem: proposal only, neither forward nor fitted — unexecuted.
- **NP08** — Triple-alpha packing. **SPEC ONLY** (PROMPT.md).
- **NP09** — Deuteron binding from occlusion. Recompiled, byte-identical. **HONEST FAIL** (−97.3%, reported as FAIL); one INFLATED cross-ref calling NP17 "exact" (fault #1, MINOR — worst in region).
- **NP10** — Alpha binding tetrahedral. **SPEC ONLY** (PROMPT + DATA_REQUIREMENTS, no tool). Depends on NP09; unexecuted. (NP17 covers the alpha and honestly marks it PENDING/under-binding.)
- **NP11** — Neutron lifetime. **SPEC ONLY** (PROMPT.md). τ_n is NOT derived anywhere; NP04A openly labels the barrier class F. No fit to 880.

## Verdict line

> **Prompt completion A** (all 11 NP audited; every C++ tool + NP05 Python compiled & run; outputs
> reproduced) · **Physics class of the region: honest OPEN/NEGATIVE with earned partials** — NP04A
> keystone (C, parameter-free) and NP17 deuteron (near-PASS, −1.06%, no fitted scale) are the real
> assets; NP05 is a clean KILL; the famous targets (deuteron occlusion, alpha, neutron lifetime,
> lithium) are correctly UNPAID or unexecuted. · **Recovery: N/A — no fabrication to retract.** ·
> **Cascade root:** the region's live open problem is a *forced nuclear geometry* (the `s`/mesh that
> NP05 killed the volume-price on, and NP17's alpha "tetrahedral lock") — owed to ROOT-SIM / the
> gear-frequency successor NP06, not yet built.

— HUNTER · NP01–11 · 2026-07-02
