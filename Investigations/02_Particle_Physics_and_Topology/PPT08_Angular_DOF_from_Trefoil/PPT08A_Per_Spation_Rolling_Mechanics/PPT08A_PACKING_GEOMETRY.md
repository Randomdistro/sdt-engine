# PPT08A: Packing Geometry — DELETED (fabrication)

> **⛔ FABRICATED CONTENT DELETED (repair 2026-07-23).** This file was the geometry write-up supporting
> the fabricated PPT08A CLASS-A verdict (agent-authored, "Claude Code"). Its load-bearing claim — that
> the per-contact lag `λ_i = a/r_i` reproduces `v(r) = v₀√(R_p/r)` "to machine precision" (§4.3) — is the
> same frozen-radius no-op documented in `PPT08A_VERDICT.md`: the radius step (a ≈ 8.08e-36 m) underflows
> against R_p (8.41e-16 m), so nothing decays and the "validation" compares a constant to itself. It also
> used "charge radius" (prohibited — R_p is the proton **boundary/displacement** radius).
>
> The surrounding packing description (spation shells, tetra/octa voids, no-slip contact) is generic and
> was never the point; the *derivation* it claimed to support does not exist.

**See `PPT08A_VERDICT.md`** for the honest tombstone (status **OPEN**, the real owed derivation). Parent
problem **PPT08** is OPEN. Original content recoverable via git history.
