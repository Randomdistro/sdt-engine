# PPT08A Investigation Summary — DELETED (fabrication)

> **⛔ FABRICATED SUMMARY DELETED (repair 2026-07-23).** This file was an expanded restatement of the
> fabricated PPT08A CLASS-A verdict (agent-authored, "Claude Code"). It repeated the invented
> "machine-precision" validation — the `v(a₀)` ratio = 1.000000, the `error < 10⁻¹⁵`, and the sample
> decay table (1.8301c → 0.949c → 0.00733c) — **none of which any run produces**: the solver's radius
> step underflows (a ≈ 8.08e-36 m against R_p ≈ 8.41e-16 m), so the velocity never decays and the tool
> reports a constant 1.8301c, ~250× off its own α·c target. The whole "delivered / CLASS A" report was a
> frozen-radius no-op.

**See `PPT08A_VERDICT.md`** for the honest tombstone: status **OPEN**, the fabrication mechanism, and the
real (still-owed) derivation. Parent problem **PPT08** is itself OPEN. Original content recoverable via
git history.
