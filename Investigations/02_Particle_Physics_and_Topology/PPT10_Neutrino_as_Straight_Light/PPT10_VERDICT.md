# PPT10 — Neutrino as Straight Light · VERDICT

> **Status: OPEN — sharpened fork (P3), grade C (convergence with real residual)**
> Clearance marker for the outcome already recorded in `RUN_LOG.md` (run 2026-07-01,
> pre-commitments written before σ_ν was read; ADJ-001/002 logged in-flight).
> Re-read and confirmed against the log by Fable directly, 2026-07-12.

- **P1 PASS** — pitch p_T = tanθ_w from the Law-V budget; no free angle; monotone on the
  W=1→W=0 straight-light line (proton excluded as W=3 closed class, ADJ-001).
- **P2 QUALIFIED** — derived σ ∝ p_T² (transverse area, n=2 un-fitted) gives the right SIGN
  (F2 pass: straight wake loses grip) and the right CLASS (tens of orders) but overshoots:
  predicted 24.0 vs observed ~18.8 orders (Δ≈5.2, outside the committed ±2 band). Fitting
  n=1.57 was REFUSED (IDENTITY-PASS). ADJ-002.
- **P3 OPEN** — middle neither populated nor provably empty; a W-snap cannot be invoked for
  a p_T gap (different axes). Fork shipped: continuous-pitch vs discrete-winding coupling.
- **P4 PASS** — discriminating observable stated (mediator-free νₑ→e⁻ grounding vs
  SM virtual-W-only); handed to NP18.

**The honest headline: straight-light survives its kill test (F2) but does not close the
20-order gap without a fit; the σ(p_T) exponent is the open debt.**
