# NP06 — The Gear Frequency as the MOVEMENT of the Binding

> **Author:** J. C. Harvey, Melbourne. **Status:** RE-OPENED and REWRITTEN 2026-07-23 (direct).
> **Prior agent kill VOIDED.** The agent-era verdict, tool and results have been deleted from this
> folder. They killed `E_bind = ℏΔω` for "storing no energy in a rigid no-slip mesh" — a **category
> error**: it demanded that a *movement* description independently generate an *energy scale*.
> Movement is not a substance. Per the framework's own rule (FARMER PASS/BECOME/PRESS —
> *pressure is movement that cannot move*), the frequency and the binding are **one fact in two
> languages**, not two competing mechanisms.
> **Engine:** `#include <sdt/laws.hpp>` only. **Propose-only** on canon flags.
> **⚠ OWED POINTER:** Harvey reports the gear frequency has *since been shown* to be the movement of
> the binding. Locate and cite that result before running; this prompt is written to receive it.

---

## ⓪ The Golden Rule — five questions (answered)

1. **What don't we know?** — **Whether the two accounts of one binding agree quantitatively.**
   NP17 gives the binding as an *energy*: a shared-electron Coulomb well, scale `αℏc`, deuteron at
   −1.06% with no fitted constant. The gear frequency gives the same binding as a *movement*: `Δω`,
   the change in meshed circulation when structures lock. The open question is not "which is right"
   — it is **does `E_well/ℏ` equal `Δω` read off the mesh geometry?**
2. **Why does it matter?** — If they agree, binding has a complete description in movement terms
   with **no energy primitive at all** — which is the framework's whole ontology (mass, force and
   binding as movement, not stuff). If they disagree, one of the two accounts is wrong and the
   disagreement localises which.
3. **How will we find out?** — Convert NP17's well depth to a frequency and compute the mesh `Δω`
   independently from the packing geometry; compare. Then repeat across d → He-3 → α, where NP31's
   orbital reach sets which contacts move together.
4. **What would prove us wrong?** — §⑧. If `E/ℏ` and the geometric `Δω` differ by more than the
   committed tolerance and the gap is not traceable to a named geometric quantity, the movement
   account fails **as an account** (not as an energy source — that test is forbidden here, it is
   the voided kill).
5. **How will we know we're done?** — Dual verdict. A PASS is a *correspondence*: the same number
   from both languages with zero fitted quantities.

---

## 1. The corrected premise (read before anything else)

**Forbidden question (the voided kill):** *"Does the kinematic mesh store enough energy to bind?"*
This asks movement to be a substance and can only ever return "no stiffness ⇒ κ = 0 or O(1)".

**The right question:** *"Is `Δω` the movement signature of the well NP17 already pays?"*

The relation under test:

```
E_bind  =  ℏ · Δω            (one binding, two languages)
E_bind from NP17 : shared-electron Coulomb well, scale αℏc, no fitted constant
Δω      from mesh: change in meshed circulation rate when the structures lock
```

**Concrete first check (computed 2026-07-23, direct):** for the deuteron,
`Δω = E/ℏ = 2.224 MeV / ℏ = 3.38×10²¹ rad/s`. The proton rim rate at the 1.83c seat is
`Ω_rim = 1.83c/R_p = 6.52×10²³ rad/s`. Ratio **Δω/Ω_rim = 5.2×10⁻³**. Whether that ratio is a
*derived* mesh quantity (and what it is) is the investigation's first real target — it is stated
here **before** any fitting, and it must not be tuned.

## 2. Why NP17 is not a "successor"

The agent verdict framed NP17 as the successor that replaced NP06. That framing is void. NP17
supplies the **energy** side and NP06 the **movement** side of the same binding. They are
complementary and must agree; neither supersedes the other. NP31 (orbital reach) supplies the
inventory of which contacts participate, and therefore which circulations lock.

---

## ④ Strategy — three gated phases

### P1 — the correspondence on the deuteron (anchor)
- Compute `Δω` from the mesh geometry (two trefoils locked at `D = 2R_p + R_p/3`, shared electron
  at the node) **without** using the measured binding.
- **Gate:** geometric `Δω` matches `E_well/ℏ = 3.38×10²¹ rad/s` within **±5%**, zero fitted inputs.
- **Forbidden:** deriving `Δω` from `E` and calling the agreement a result (that is an identity).

### P2 — the ratio `Δω/Ω_rim`
- Determine whether `5.2×10⁻³` is a named geometric/kinematic quantity of the interleaved packing.
- **Metric:** expressed in native quantities (`W`, `ϟ`, coordination `k`) with no free number, or
  declared **OPEN** with the reason.

### P3 — across the light chart
- With NP31's coordination `k`, predict `Δω` for He-3 and α and compare to their well depths.
- **Metric:** correspondence holds to ±10% with no per-nucleus adjustment.
- **Cross-gate:** must not break NP17's deuteron or NP31's saturation.

---

## ⑧ Falsification tests

| ID | Hypothesis | Predicted | If FAIL → root |
|---|---|---|---|
| T1 | geometric `Δω` = `E_well/ℏ` (deuteron) | 3.38×10²¹ rad/s, ±5% | root-1 (the two accounts disagree) |
| T2 | the correspondence is not an identity | `Δω` computed without `E` | root-4 contraband (circular) |
| T3 | `Δω/Ω_rim` is a native quantity | expressed in `W, ϟ, k` | OPEN if only measured |
| T4 | correspondence holds A = 2,3,4 | ±10%, no per-nucleus tuning | root-1 or root-2 (NP31) |
| T5 | no energy primitive is needed | binding fully stated as movement | root-3 if an energy must be posited |

**Explicitly NOT a falsifier:** "the mesh has no stiffness." That is the voided kill and may not be
re-run as a test.

## ⑨.E Four-root sort
root-1 claim false (the two languages give different numbers) · root-2 dependency (NP31 coordination
/ NP17 well) · root-3 prompt under-specified (`Δω` definition ambiguous) · root-4 contraband
(circular derivation of `Δω` from `E`; or re-importing "movement must store energy").

## Brainstorm — overlooked factors
- **The identity trap is the main risk.** `Δω = E/ℏ` is trivially true if you compute `Δω` from `E`.
  The entire content is computing `Δω` from *geometry* and finding it lands on the same number.
- **`Δω/Ω_rim = 5.2×10⁻³` is suspiciously near α (7.3×10⁻³) but is NOT α** (29% off). Do not fish it;
  state it, test it, and reject it out loud if it is a coincidence.
- **This is where "binding is movement" becomes decidable** rather than a slogan — the framework's
  strongest ontological claim gets a number attached, or it does not.

## ⑤ Success / ⑥ Outputs
Class A: geometric `Δω` = `E_well/ℏ` on the deuteron and across A=2–4, zero fitted, ratio named.
Class C: correspondence with ≤1 documented approximation. **Honest floor:** correspondence stated
and tested on the deuteron only, ratio OPEN. Outputs: `RUN_LOG.md`, `np06_gear_movement.py`,
`NP06_DERIVATION.md`, `NP06_VERDICT.md`.

---

*NP06 · re-opened 2026-07-23 · the gear frequency is the movement of the binding, not a rival to it;
the agent kill asked movement to be a substance and was voided. The live question is the
correspondence, and it has a number: 3.38×10²¹ rad/s.*
