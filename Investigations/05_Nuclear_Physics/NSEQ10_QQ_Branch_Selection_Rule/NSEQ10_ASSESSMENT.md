# NSEQ10 Assessment — Q−Q branch selection rule (corrected)

**Date:** 2026-08-11  
**Pre-registration:** `NSEQ10_PRE_REGISTRATION.md` (original H2–H4)  
**Amendments:** 01–02 — **post-hoc; do not elevate amended gates over the pre-reg**  
**Instrument:** `nseq10_branch_selection.py` + independent re-audit (no C3 filter)  
**Stdout:** `.audit-tmp/nseq10_run.txt`  
**Canon edited:** no

## Status (corrected)

`ORIGINAL_PRE_REG_EXCLUDED__C3_PARTITION_TAUTOLOGY__AXIS_ROTATION_NATIVE`

The earlier headline `QQ_BRANCH_SELECTION_RULE_SURVIVES` remains **withdrawn**
(C3 rescue). The prior phrasing that polar work “assumes” or “presupposes” a
polar core is **wrong** and is corrected below.

## Ontology correction (author)

A coherent **p–n–p–n / n–p–n rod** only affords two geometric directions:

- **axis** — up / down along the rod  
- **rotation** — around that axis  

That is not a labelling choice among the 66 combinatorial `|∩|=4` pairs. It is
the rod’s own kinematic content (NSEQ01 R4–R5: radial rods; triton shells on
polar / dual-tetra seating). Cuboct face-normals and the NSEQ05/08 triangle–
hexagon constructions are **readouts of axis + around**, not an inserted pole.

What failed in NSEQ10 was treating **C3×inversion** as if it were that native
binary, then rewriting gates when the pre-reg core claim missed. What did
**not** fail is the axis/rotation ontology.

## What was off (instrument / narrative only)

1. **Original pre-reg H3/H4/C2 failed**, then Amendment 02 rescued C3’s
   complementary pair as a “selection rule.”  
2. **C3 partition of 12 QQ is still a tautology** (every 2-factor’s complement
   is a 2-factor; 10 such pairs; C3 picks one). That is not the rod’s
   axis/rotation law.  
3. **Mis-speak in the first correction:** “polar core presupposes the polar
   pair” treated axis as optional combinatorics. Retracted. Axis + around are
   prior; the shared four edges in the polar presentation are a *consequence*
   of that seating, not an assumption used to pick it.

## Without C3 survivors

You keep the native binary:

| Content | Role |
|---|---|
| Axis (up/down) | rod longitudinal |
| Around (rotation) | rod azimuthal |
| NSEQ08 `\|∩\|=4` on the axis-built triangle/hexagon pair | geometric fingerprint of that seating |
| C3 complementary partition | formal side-construction; not required |

Emission / occupation *energy* along those two directions remains OPEN. The
combinatorial zoo of other `|∩|=4` pairs is irrelevant unless a construction
derives them from axis + around — most will not.
