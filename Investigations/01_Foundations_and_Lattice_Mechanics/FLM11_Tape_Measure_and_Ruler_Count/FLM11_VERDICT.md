# FLM11 — Verdict (dual)

> **Author:** J. C. Harvey, Melbourne. **Run:** 2026-07-01.
> Tool: `flm11_ruler_count.py` → `flm11_results.txt` (exit 0, all gates met). Full argument: `FLM11_DERIVATION.md`; log: `RUN_LOG.md`.
> Inputs: derivation basis + `law_VI` integers only. Zero fitted parameters (CALIBRATED budget 0, unused).

## Prompt completion: **C** — all four phases (P1 Lemma → P2 count → P3 residue → P4 map) executed as written; the DoF→property *assignment* rests on 1–2 argued grippability claims, so not an unqualified A.

## Physics class: **CONVERGENCE** (count ties physics' four; residue is a native novel structure) · with a **NATIVE** core (the integer 4+1 and the zero-grip residue fall out of the form's DoF).

---

**The result.** Enumerating the trefoil/vortex form's externally grippable configuration axes and applying only native constraints yields **P = 4 grippable rulers** — `{mass (RESISTANCE / Law IV), charge (HANDEDNESS / Φ₃), magnetic flux (CIRCULATION / v_T), EM (OSCILLATION / Φ₂)}` — **plus exactly one zero-grip residue**, the **through/straight axis**, which is the **W=0 open line = the neutrino**. Physics' four "fundamental properties" are not facts about objects; they are the four *kinds of purchase* a committed strip can take on the one medium. The neutrino is the DoF with no ruler cut from the same cloth.

**The falsifiable core, both parts settled:**
1. **Derived integer count = 4 (+1 straight).** Pre-committed in `RUN_LOG.md` *before* the tool ran; the tool reproduced it with no axis added or removed after seeing physics' "four."
2. **Fifth independent ruler: NO.** The only continuous candidate, poloidal `v_P`, is removed by the **Law V budget** `v_T²+v_P²=c²` (tool residual = 1.000000000000, exact) — the answer is not what kills it; a native constraint is.

**Why CONVERGENCE, not a clean Class A NATIVE.** The *count* is native and the *residue* is a genuine native prediction, but the **map from each surviving DoF to a specific named property**, and the **completeness of the externally-grippable set**, are *argued*, not proven (which Level-5/6 components an outside strip can grip is a stated assumption). Honest label: NATIVE core, CONVERGENCE overall. A count that merely *tied* a Lie-group dimension would be pure CONVERGENCE; this is stronger (it ties a *counting of physics' own properties*) but weaker than a proven grippability theorem.

---

## §8 Falsification table — actual outcomes

| ID | Hypothesis | Predicted | **Actual outcome** | Verdict |
|----|-----------|-----------|--------------------|---------|
| **F1** | Property count = independent-ruler count | 4 grippable, each → one physics name | Count = 4; one-to-one map to {mass, charge, flux, EM} | **PASS** |
| **F2** | Straight projection is one of the form's DoF | exactly one zero-grip axis = through = W=0 | exactly 1 zero-grip; = straight; maps to W=0 (tool: 3× PASS) | **PASS** |
| **F3** | Count is a clean unambiguous integer | every axis decisively in/out by a stated constraint | 4 in (grip taxonomy), 1 dependent (Law V), 1 residue, 2 excluded (stated) — no ambiguous axis | **PASS** |
| **F4** | `v_P` is *not* an independent ruler | Law V fixes `v_P` given `v_T` → flux is one ruler | budget closes to 1.000000000000 (electron & proton) → `v_P` dependent | **PASS** (no fifth ruler) |
| **F5** | Committed cloth cannot read Π_∥ | only uncommitted cloth (BEC/NP18) reads it, by coherence not grip | Π_∥ has grip=NONE by construction; no committed strip couples | **PASS** (structural, not experimentally closed — see caveat) |

**Caveats / owed.** F5 is argued structurally (a committed strip is basis-pinned; Π_∥ presents no basis direction to pin to), not demonstrated by exhibiting a failed instrument — it inherits PPT10/NP18. F1's *assignment* (T₂→mass, Φ₃→charge, v_T→flux, Φ₂→EM) and the *completeness* of the grippable set are the soft joints; they are the Class-C reason. No falsifier fired FAIL; none was silenced.

---

## STRESS-TEST SELF-AUDIT

**(1) Independent, or identity / circular?** — **Independent.** Physics' "four properties" is **not an input**: the count P=4 was written into `RUN_LOG.md` *before* the tool ran, and it is produced by (a) a closed taxonomy of four grip mechanisms and (b) the **Law V budget** removing the fifth candidate `v_P`. The thing that kills the fifth ruler (v_T²+v_P²=c²) is *not* the target — no identity-pass, no `engine_fn() vs CODATA` masquerade. The W=0 ↔ neutrino step reuses the header's existing mapping, so *that* identification is inherited, not re-derived (flagged).

**(2) Fished?** — **No number was fished.** There is no fit, no tuned exponent, no tolerance (CALIBRATED budget 0, unused). The only "numbers" are the exact budget residual (1.000000000000) and small integers (W=0/1/3, T=12, P=4). Contrast the FLM12-D1 bar, which honestly returned UNPAID for a *fished* squared-exponent; here nothing analogous was tried — the count is combinatorial, not fitted. The exclusions (G kinematic position, H orientation frame) were stated on structural grounds pre-run, not removed to reach 4.

**(3) Dimensions / logic.** — The Tape-Measure Lemma is exact linear algebra (`⟨L,a⟩` returns only `cₐ`). The count is pure integer/boolean logic. The one dimensional check — Law V's `(v_T²+v_P²)/c²` — is dimensionless and closes to 1 exactly. No borrowed units (no μ_B, no magnetons), no G/M/GM, no charge-as-substance ("handed redirection" used throughout), no gauge-group dimension imported as the derivation. Logic is sound; the *soft* step is semantic, not dimensional: whether the four named grip mechanisms are genuinely exhaustive and whether each 28-D component is externally grippable.

**(4) Honest class + what is owed.** — **Prompt completion C; Physics class CONVERGENCE (NATIVE core).** **Owed:** (i) a *proof* that the four grip mechanisms are exhaustive (not just enumerated); (ii) a *proof* that T₂/Φ₃/v_T/Φ₂ are the complete externally-grippable subset of Levels 5–6 (grippability is asserted); (iii) an operational demonstration of F5 (that no committed instrument reads Π_∥), which is deferred to PPT10/NP18. The **strong, falsifiable core is paid**: the count is a clean derived 4+1, the fifth-ruler answer is NO by an exact native constraint, and the straight residue is the unique unmeasurable = the neutrino. It is a **correct structural result with two argued (not proven) joints** — not forced to 4, and not a fake PASS.
