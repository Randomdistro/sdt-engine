# FLM13 — DERIVATION NOTES

> J. C. Harvey, Melbourne · executed 2026-07-03 · companion to `FLM13_VERDICT.md` and
> `flm13_results.txt`. Every line flagged DERIVED / IDENTITY / OPEN per R5.

## 1. The ROLE (DERIVED — mechanical, not postulated)

In a superfluid, single-valuedness of the condensate phase around a vortex core forces
`∮ v·dl = n·h/m` (Onsager–Feynman). SDT's substrate is a reconfigurable superfluid and its
particles are quantised vortices (OP-2), so the W=1 unknot — the electron — carries exactly
one circulation quantum:

```
κ = h/m_e         ⇒    ℏ = m_e·κ/2π
```

The koppa ladder's bottom rung IS that vortex: radius a₀, speed αc. Its mechanical
circulation 2π·a₀·(αc) equals κ, hence ℏ = m_e·(αc)·a₀ = r_e·m_e·c/α. **Bohr's L = nℏ is
recovered as vortex circulation, not assumed.** [numeric: three forms = ℏ·1.000000001]

## 2. Why the VALUE does not follow (IDENTITY, flagged)

`a₀ ≡ ℏ/(m_e c α)` and `r_e ≡ αℏ/(m_e c)` are definitions *in* ℏ. Substituting them back
returns the ℏ you inserted — ROLE, not extraction. The delete-test table
(`flm13_results.txt` P2) shows every dimensionful atomic input encodes ℏ, including
m_e-in-kilograms post-2019 (the kg is now *defined* via fixed h). Only α (pure ratio) and
c (Mercury inversion / SI definition) are clean — and no set of lengths, times and pure
numbers can manufacture the Mass dimension. **No ratio is an action.**

## 3. The collapse (DERIVED relations; one CONSISTENCY exhibit)

- W=1: `ℏ/m_e = κ/2π = (αc)·a₀` — the action-to-mass *ratio* is fixed by rung geometry and
  is measurable kilogram-free (photon-recoil h/m).
- W=3: `R_p·m_p·c = (W+1)·ℏ = 4ℏ` — engine relation, lands 4.0008 (+0.0195%).
- Eliminating ℏ: `m_p/m_e = (4/α)·(r_e/R_p)` = 1835.79 vs measured 1836.153 (−0.0195%).
  The two residuals are one fact (algebraically the same closure inverted). This exhibits —
  does not derive — that the three quantities carry **one** degree of freedom.

## 4. The frontier (OPEN, with the reason standing)

Macroscopic routes (He-II κ, flux quantum, Josephson) deliver h/m and h/2e — ratios.
Converting any of them to an absolute action requires a mass anchor; the only ℏ-free mass
anchor ever used was the artifact kilogram, i.e. **a counted assembly of baryons**. The
Seed Theorem (FLM06) survives in sharpened form: *the one unavoidable dimensional anchor
is a baryon count.* Cross-reference: GOM14 (same day) shows the celestial sector running
entirely on the koppa/count/ratio chain; FLM12 owns what the per-baryon seat *is*.
