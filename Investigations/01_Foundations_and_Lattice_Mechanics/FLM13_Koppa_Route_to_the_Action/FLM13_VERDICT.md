# FLM13 — VERDICT

> **Author:** J. C. Harvey, Melbourne. Executed by HUNTER, 2026-07-03.
> Tool: `flm13_action.cpp` (BUILD_EXIT=0, RUN_EXIT=0; `flm13_results.txt` = real stdout).
> Pre-commitments honoured verbatim (`RUN_LOG.md` written before the tool).

## Dual verdict

> **Prompt completion A · Physics class NATIVE (ROLE + one-seat collapse) · VALUE: OPEN
> behind the Seed Theorem, exactly as pre-committed · Recovery: n/a · Cascade root: the
> mass/action seat — handed to FLM12.**

## What was earned

- **P1 (ROLE).** ℏ is the W=1 rung's circulation action: `m_e·κ/2π = m_e·(αc)·a₀ =
  r_e·m_e·c/α` agree with ℏ to 1.000000001 (gate ≤1e-6 PASS). **Labelled IDENTITY-CLASS**
  (a₀, r_e are ℏ-defined lengths) — this derives what ℏ *is* (the Onsager–Feynman
  circulation quantum of the fundamental vortex), not its number (R5 respected). The
  proton-geometry path lands 1.000114 — residual = stored-constant rounding + R_p
  measurement, labelled as such.
- **P2 (delete-test).** The full input table is printed: every dimensionful atomic input
  (r_e, a₀, R_p, ϟ_H, E_ion, and post-2019 even m_e-in-kg) **encodes ℏ**; only α and c are
  ℏ-free. Verdict: **no ℏ-free VALUE path exists**, with the dimensional theorem as the
  standing reason — no combination of lengths, times and pure numbers manufactures the
  Mass dimension; no ratio is an action.
- **P3 (collapse).** `{ℏ, m_e, m_p}` → **ONE seat**, exhibited numerically:
  bijection 1 (W=1): ℏ/m_e = κ/2π = 1.15768e-4 m²/s — a **kilogram-free observable**
  (photon-recoil h/m class); bijection 2 (W=3): `R_p·m_p·c/ℏ = 4.0008` (+0.0195% vs W+1=4).
  Eliminating ℏ between the two rung relations gives the pure-ratio statement
  `m_p/m_e = (4/α)(r_e/R_p) = 1835.79` (−0.0195% vs 1836.153). **Honesty note: those two
  0.02% closures are algebraically the SAME residual seen from both ends** (the cross-check
  is the W+1 relation divided by the measured mass ratio) — one consistency fact, not two.
- **P4 (frontier).** He-II circulation and flux-quantum routes yield only *ratios*
  (h/m, h/2e), never an absolute action. The one historically ℏ-free mass anchor was the
  artifact kilogram — **a counted lump of baryons**. So the irreducible seat is one
  macroscopic **baryon-count anchor**, which the ladder then distributes via W=1 and W=3.
  This closes the loop with the ratio-ontology result of the same day (GOM14): the theory
  runs on lengths, times, counts and ratios, **plus exactly one ruler** — and the ruler is
  a count of matter, not an abstract constant.

## Reduced whitelist (on the record)

`{ ℓ_P ← koppa(g,ρ,R) · c ← Mercury precession · α ← pure spectral ratio ·
ONE mass/action seat (ℏ ≡ m_e ≡ m_p via W=1/W=3) → FLM12 · k_B, T_CMB (thermal seat) }`

The classical-sector compression 8 → 4+thermal is complete. FLM13's job ends at the
collapse; what the seat *is* belongs to FLM12 (mass = field-resistance to vector change).

## Falsification table (§⑧)

| ID | Outcome |
|----|---------|
| F1 | PASS — three forms agree ≤1e-6; circulation stated as Onsager–Feynman superfluid quantisation of the W=1 unknot, not a Bohr postulate |
| F2 | CONFIRMED — no ℏ-free path; dimensional theorem stated; nothing overturned it |
| F3 | PASS — both bijections exhibited numerically; exactly one seat survives |
| F4 | RESPECTED — no identity shipped as extraction; all identity lines labelled |
| F5 | CONFIRMED — Seed Theorem stands; the seat is a baryon-count anchor |
