# FLM12 — Verdict (dual)

> **Author:** J. C. Harvey, Melbourne. Full argument: [FLM12_MASS_MECHANISM.md](FLM12_MASS_MECHANISM.md).
> Tool: `flm12_mass_field_resistance.cpp` → `flm12_results.txt` (MSVC/GCC, `#include <sdt/laws.hpp>`).

## Prompt completion: **A** (mechanism determined; anomaly field scoped and sorted)
## Physics class: **NATIVE** (ontology + mechanism) · **OWED** (mass values) · **OPEN** (3 named)

---

**The determination.** Mass is the spation field's **resistance to a change in a form's vector**,
delivered by the convergent influx arriving isotropically at `c` in discrete quanta; mass is the
coefficient by which that influx pushes back under acceleration (relay-Doppler imbalance). Nothing
carries mass — a form *commands* it, in proportion to the influx it engages (Law IV). This recovers
**Newton I, Newton II, `E=mc²`**, and — its sharpest, already-passed test — the **equivalence
principle as a theorem** (`m_inert ≡ m_grav`, MICROSCOPE `η<10⁻¹⁵`), and reframes **every mass defect
as a synchrony saving**.

**Why it was forced.** The confinement objection (`ℏc/Δx ≈ 235 MeV` to localise an electron to `R_p`,
vs `~1 MeV` bindings) proves mass **cannot** be a localised substance. Field-as-resistance is the only
option left under SDT strictures — and it **dissolves** that objection, not patches it.

**The anomaly field, sorted (see `flm12_results.txt`):**
- **DISSOLVED (5):** equivalence principle (→ theorem), mass↔energy "mystery", confinement, the
  vacuum-energy `~10¹²³` catastrophe (category error — no energy substance is posited), and the muon
  `g−2` residue (now a *lattice-vs-data-driven hadronic-vacuum-polarisation* dispute = how you compute
  the field).
- **NATIVE (4):** loose-electron magnetism, negative neutron-moment sign, `μ_p≠1`, the binding curve as
  a resistance landscape with the Fe/Ni floor.
- **RE-DESCRIPTION (3) + RESOLVED CONSTRUCTION (1):** Koide `Q≈2/3`,
  lepton ratios, and exact neutron moment/`Q_β` remain accommodated;
  `m_p/m_e = 6π⁵` is construction-resolved by PPT17 as a shared-input
  point-electron-tube → proton-torus volume map.
- **OPEN (5):** deuteron binding *from* synchrony (D1, the priority test), the iron floor computed
  (D2), the neutron `G_F`/lifetime (D3), the `α` Cs–Rb `5.4σ` split (D4), neutrino oscillation (D5,
  hardest).
- **RESOLVED, not evidence (2):** W-boson mass (CDF is an outlier), proton-radius puzzle (resolved to
  the muonic `0.8414 fm` = SDT `R_p`).

**Honest bottom line (updated 2026-07-02, two runs).** The *mechanism* is determined; the *numbers*
are owed.

**D1 (force picture with the EM `P_eff`) FAILED** — occlusion undershot the deuteron by `15–70×`;
`αℏc` hit `2.224 MeV` only by localising a mode into a `~100 MeV` well SDT couldn't supply. It looked
like *"SDT has no strong force."*

**D1′ (influx-regime recovery, Harvey) CORRECTED that:** occlusion's `P_eff` is regime-dependent, and
the influx a *nucleus* shadows is the convergent boundary relay engaged at the nuclear sphere —
`m_p c²/V_p = 6×10³⁴ Pa`, `~380×` the EM value (grounded in Law I + Law IV, computed independently of
the target). With it, scalar occlusion reaches `25–76 MeV`: **the nuclear scale is recovered — the
strong scale IS the CMB influx read at the nuclear sphere, not a missing force.** But it **overshoots**
the anomalously-loose deuteron `~11–34×`; the two regimes now **bracket** `2.224 MeV` (EM `33×` under,
nuclear `11×` over) — **scale recovered, exact value NOT predicted** (owed to the deuteron's loose
geometry; a geometric-mean "hit" was tested and rejected as regime-fragile numerology).

**Net:** the ontology stands and is reinforced; the "no strong force" verdict was **premature**; the
strong *scale* is now grounded in the influx regime; the *exact* deuteron binding is bracketed and
owed to geometry (**D1″**) — with topological linking (NP17's "lock") the other live route. Two
independent computations, no result moved.


## D1 direct result

> **Run:** 2026-07-01 · `flm12_d1_deuteron_synchrony.py` → `flm12_d1_results.txt`. Inputs: derivation basis +
> measured anchors (R_p, D_deuteron, m_N, deuteron binding as the TARGET). Zero fitted parameters.

## Verdict: **D1 does NOT pay out — UNPAID (housed, not derived).**

FLM12's own make-or-break test: does the deuteron binding **fall out** of "mass defect = field-resistance
saved when two forms synchronise"? Run honestly, it does not.

**What the numbers say:**
- At the measured separation **D = 1.94 fm the nucleon hard-boundaries (R_p) do not even touch** (2R_p = 1.68 fm;
  gap 0.26 fm). So there is **no boundary overlap** — the binding must be a **far-field influx effect** whose
  profile `ρ_eng(r > R_p)` the mechanism **does not specify**. This is the same open kernel as the FLM03⇄FLM10
  bridge (the nucleon influx/wake profile).
- The **principled** readings of "engagement-density × overlap-volume" span **0.51 → 44 MeV** — they *bracket*
  2.224 MeV but do **not predict** it. The naive solid-angle influx share **over-binds ×20** (44 MeV).
- The only expression that **lands** is the **comparator** `3αℏc/D = 2.227 MeV` — but that is **NP17's
  electrostatic geared-bond mechanism, a different mechanism**, not the synchrony route. The synchrony picture
  does not independently reproduce it.
- A **squared** solid-angle form gives 2.08 MeV (7% off), but its exponent was **fished** — tried until it
  landed. That is numerology, **flagged, not claimed**.

## Consequence

The synchrony mechanism can **accommodate** 2.224 MeV (with a chosen influx profile or exponent) but does not
**derive** it. Per FLM12's own ledger, **D1 stays OPEN and the quantitative ledger stays unpaid** — FLM12
remains a determined ontology with no derived number. To pay D1, the framework must first fix the nucleon
**engagement/influx profile `ρ_eng(r)` beyond R_p** (FLM03⇄FLM10); until then D1 cannot be a PASS without
fitting, and no fit was performed here.

**Class:** FAIL (naive/principled forms) → **OPEN** (pending `ρ_eng(r)`). No result was moved or fished into a pass.
