# FLM14 — The Rotating Spation & Sequential Occupancy

> **Author:** J. C. Harvey, Melbourne. **Status:** SPEC (committed 2026-07-04 — "I am going
> all in on this one").
> **Inherits:** `PERFECT_PROMPT_TEMPLATE.md` §⓪–§⑩ · `PROMPT_EXECUTION_PROTOCOL.md` ·
> §0 anti-creep (R0–R5) · `HUNTER_PROTOCOL.md` (kill-list, seven questions, cascade ledger).
> **Engine:** `#include <sdt/laws.hpp>` (+ `lattice_structure.hpp` for FLM08 geometry).
> **Run:** Pre-commit thresholds in `RUN_LOG.md` before coding.

---

## ⓪ The Golden Rule — five questions (answer before Phase 0)

1. **What don't we know?** — Whether the spation carries ROTATION as a real degree of freedom
   (an internal phase/clock, geared cell-to-cell), and whether electron positions are
   **sequential** — a deterministic, gear-stepped tour of seats — rather than a static
   distribution. This is the shared premise under every major open in the ledger.
2. **Why does it matter?** — It is the candidate **root-payer**. If the spation rotates and
   occupancy is sequential, then: the residence law (RESIDENCE-LAW root: QM04 Born weighting,
   QM05 Bell joint readout, QM07 sequential-SG) becomes a dwell-time ledger; the Lamb spacing
   (PPT08) becomes S-route vs P-route geometry; the nuclear lock (NP17-PENDING, ×1.01/×1.98/
   ×6.35) becomes angular-window sequencing; FLM03's co-rotation coefficient gets its
   mechanism; and ε = hν (TD-ROOT-3, carrying ALL quantum content in TD06) becomes movement
   per rotation step — a native route to the action. One premise, six debts.
3. **How will we find out?** — Build the minimal rotating-cell model on the FLM08 lattice and
   let SEQUENCES, not fields, generate the observables. Grade each against its pre-committed
   table (§⑧). No observable may be fed its own answer.
4. **What would prove us wrong?** — §⑧: any of the three ladders missed simultaneously; a
   sequence law that must be re-tuned per observable (knob, not mechanism); rotation breaking
   FLM10 isotropy; or the sequence reproducing Born/Bell only by importing the QM weighting
   (CONSTRUCTION, not derivation — the QM05 lesson).
5. **How will we know we're done?** — **Dual verdict** per phase + the canonical HUNTER line.
   The all-in is on the PREMISE; every numerical claim still gates individually.

---

## §0 Anti-creep (R0–R5) — additions specific to FLM14

- **R-seq:** the sequence must be DETERMINISTIC given initial phase; probability enters only
  as ignorance of phase (ensemble over starting tick), never as a postulate.
- **R-rot (amended 2026-07-04, Harvey):** spation rotation must be locally undetectable in
  light propagation (Michelson–Morley class, ~10⁻¹⁷ — hard-measured), but a **small GLOBAL
  rotation axis is PERMITTED, not fatal** — global isotropy is the rival's assumption, not a
  measured fact (§B: do not use it as a kill-standard). The measured anchors that bound (and
  may demand) a global axis: the galaxy spin-parity excess (Longo/Shamir class, few-% wide-
  survey to larger JWST deep-field values; contested, OPEN) and the CMB low-ℓ structure
  (weak quadrupole aligned with octopole — the map is the anchor; Harvey's candidate reading:
  our motion in the light cone; alternative: slow global rotation). If the stepping rule
  REQUIRES a nonzero global phase drift, that is a PREDICTION OPPORTUNITY: its magnitude must
  then land inside the spin-parity and low-ℓ brackets — graded, not assumed away.
- **R-Born:** forbidden to insert cos² / |ψ|² weighting by hand anywhere (QM04 firewall).
  The dwell-time distribution must EMERGE from the stepping rule.
- Standing rules: no ψ, no charge-substance, no borrowed units, no G/M in-chain, boundary
  radius language, honest FAIL/OPEN is success.

## 1. The claim (Harvey, verbatim intent)

The spation MUST rotate — these problems only escaped their solves because the cells were
modelled static. All electron positions are **sequential**: the electron occupies a defined
order of seats, phase-locked to the rotation of the cells that relay it. The "orbital" is a
tour, not a cloud. The pair is one tour split across two wings (geared, complementary — the
QM05 correction). The S-form and P-form are different routes past the proton, so their seats
differ — the Lamb spacing is route geometry (PPT08_KOPPA_TARGET). The nuclear lock is the
allocation of scarce angular windows to sequenced contacts (FARMER refinement 2).

**Existing in-engine evidence consistent with the premise (none of it yet a proof):**
FLM03's rotation ladder v(R_p)=1.83c → v(r_e)=c → v(a₀)=αc (the speeds a rotating relay
hands off along the koppa ladder); FLM07's 1:4 void gearing and glancing-c locus; OP-2
(substrate = superfluid; particles = quantised vortices — circulation is already canonical);
the honest PPT08 rebuild's conclusion that angular DOF must come from the NEAR field.
The fabricated PPT08A asked the right question (per-spation rolling) and faked the answer —
FLM14 must answer it honestly or kill it.

## 2. Phases (gate each; pre-commit in RUN_LOG.md)

- **P1 — The stepping rule.** Define the minimal rotating-cell relay: one internal phase per
  spation, geared to neighbours (FLM08 adjacency; FLM07 1:4 ratio as the candidate gear).
  Deliverable: the update rule, with NO per-observable parameters. Gate: rule stated in ≤5
  native quantities; FLM10 isotropy preserved (re-run the isotropy harness on the rotating
  lattice — anisotropy ≤ the static lattice's A^U within 20%).
- **P2 — Sequential occupancy → the residence law.** Run a bound tour (hydrogen seat) and
  measure dwell-time per position from the stepping rule alone. Gate (pre-committed): the
  emergent weighting reproduces the observed landing statistics (QM04's target) with ZERO
  inserted weighting; if it emerges ∝ wake-amplitude², RESIDENCE-LAW is paid — then and only
  then re-run QM05 Phase 2 with the derived law: S lands where it lands (2√2 → NATIVE;
  under → falsifiable departure; over → channel killed). All three outcomes publishable.
- **P3 — The Lamb table from routes.** S-route vs P-route seat radii from the same stepping
  rule. Gate: the THREE-SEAT table simultaneously — Δr(H,n=2)=0.272 fm, Δr(H,n=3)=0.410 fm,
  Δr(He⁺,n=2)=0.452 fm — each within 10%, no per-seat tuning. (PPT08_KOPPA_TARGET grades this;
  the R_p/3 cluster either falls out with its scaling or dies here.)
- **P4 — The lock ladder from windows.** Sequenced angular-window allocation at contact
  (NP10 corrected premise). Gate: coordination boosts ×1.01 / ×1.98 / ×6.35 (d/t/α) within
  15% from the same rule; flat per-bond factors are already falsified — reproducing them is a
  FAIL.
- **P5 — ε per step (the TD-ROOT-3 door).** Does the stepping rule fix an energy per rotation
  step that reads as ε = hν natively? Gate: ℏ's ROLE (FLM13) recovered as sequence action with
  no new anchor; if not, honest OPEN — do not force it.

## ⑧ Falsification table (pre-committed)

| ID | Test | Kill condition |
|----|------|----------------|
| F1 | Two-scale isotropy (amended) | LOCAL: light-speed anisotropy above Michelson–Morley 10⁻¹⁷ class → dead. GLOBAL: a required rotation axis is NOT a kill — but its magnitude must sit inside the measured brackets (spin-parity excess; CMB low-ℓ); outside both → dead |
| F2 | Emergent dwell law (P2) | requires inserted cos² / psi-squared weighting → CONSTRUCTION, not paid |
| F3 | Bell via derived law | marginals signal (> 0.002) → channel dead regardless of S |
| F4 | Three-seat Lamb table | any seat missed > 10% with others tuned-free → route model dead |
| F5 | Lock ladder | flat factors reproduced, or per-coordination re-tuning needed → dead |
| F6 | One rule throughout | ANY per-observable parameter change between P2–P5 → FISHED, all in doubt |

**The all-in is survivable because these gates are written before any tool.** A premise this
central earns the right to be tested hard, not the right to pass.

## Dependencies / cascade

Pays (if it lands): RESIDENCE-LAW · PPT08/Lamb · NP17-PENDING LOCK · FLM03 co-rotation ·
TD-ROOT-3 (ε=hν) · the QM05 joint-weighting debt. Consumes: FLM08 lattice, FLM07 gearing,
FLM03 ladder, PPT08_KOPPA_TARGET table, NP10 corrected prompt. Sibling: ROOT-SIM (the lattice
solver) — P1's stepping rule is a candidate core for it (STACK priority 1 confluence).

*FLM14 · SPEC · the premise under everything that stayed open. Execute with
`PROMPT_EXECUTION_PROTOCOL.md`; grade with `HUNTER_PROTOCOL.md`.*
