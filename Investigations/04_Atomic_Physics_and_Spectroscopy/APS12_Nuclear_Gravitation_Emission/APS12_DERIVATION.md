# APS12 derivation — Δz emission

**Date:** 2026-08-13. **Canon:** none. Every step tagged.

## 1. Depth

`z = ϟ / r` — IDENTITY with `bridge` / `depth_closure`.  
H-like point center: `ϟ = Z r_e` — DERIVED from `c_boundary_nuclear` / `koppa_hydrogen = α² a_0`.  
`r_n = a_0 n² / Z` — DERIVED (APS01 seat).  
`z_n = (Z α / n)²` — IDENTITY of the two previous.

Engine literals `α² a_0` and `r_e` differ by 1.94×10⁻¹² (ADJ-001). Not a fitted scale.

## 2. Line

Leading: `hν = ½ m_e c² (z_low − z_high) (μ/m_e)` — DERIVED from Law V kinetic `½ m v²` with `v = c√z`, times two-body `μ`.  
Algebra identity with the Rydberg closed form. Mechanism is not Coulomb: Law III occlusion, koppa `ϟ = Z r_e`, depth `z = ϟ/r`. Correspondence is not provenance. The hydrogenic number cannot discriminate mechanisms.

Law V remainder: `E_LV / (m c²) = [1−√(1−z_low)] − [1−√(1−z_high)]` — DERIVED, logged, not used to retune gates.

`1/λ = R_∞ (μ/m_e) Z² (1/n_lo² − 1/n_hi²)` — IDENTITY with the Δz path (two-route max rel 5.3×10⁻¹⁶).

## 3. Ionisation

Last electron: `IE_Z = Ry (μ/m_e) Z²` — same as `z_1` with upper seat at infinity. IDENTITY with H-like ionisation.

S_bare: `Z_eff = Z` at frozen `n(k)` — ASSUMED occupancy (no screening).  
S_ion: `Z_eff = k` at frozen `n(k)` — ASSUMED occupancy (perfect inner screening).  
Neither is inverted from IE.

`n(k)` = principal n of the outermost electron in an N=`Z−k+1` Madelung ground config — ASSUMED freeze, Phase 0.

## 4. Isotope

I_mass: reduced mass only — IDENTITY two-body.  
I_size: `Θ = (1/4)(R_A / r_n)²`, `R_A = nuclear_boundary_radius(A)` — ASSUMED covering-fraction proxy; NP12 `R(A)` is DERIVED packing.  
I_grammar: `n_t = A−2Z`, `n_d = 3Z−A−2` — DERIVED constitution. No extra scale.

## 5. Not in this chain

G, M, Slater σ, QED logs, `Z_eff = n√(IE/Ry)`, Lamb 1051.8, `dnn` retune.
