# RUN_LOG — TD08 Fluctuation–Dissipation and Brownian Motion

**Executor:** J. C. Harvey, Melbourne · 2026-07-03
**Committed BEFORE first compile/run. No threshold below may be widened after a number is seen.**

## Pre-Run Commitments — TD08

- Prompt completion target: **C** (prompt's own expected landing)
- Physics class hoped: the "one statistic" claim (H2) is the testable core. Pre-registered:
  the collision-stream model is a textbook Rayleigh gas; unless something lattice-specific does
  work, class is **CONVERGENCE/NULL-SDT** for the mechanism, with the honest point being that
  drag and noise are BOTH emergent from one simulated collision stream with **zero separate
  noise normalisation anywhere in the code** (the forbidden move is normalising noise to force
  D = µk_BT; here no noise amplitude parameter exists at all).
- CALIBRATED budget: 0. Whitelist: `measured::k_B` (unit bridge). Units m = k_B = 1.
- Model: 1D Rayleigh gas. Heavy vortex M = 100 m in a bath of TD02-MB particles at temperature
  T; collisions generated by exact flux-weighted thinning (candidate partners at Poisson rate
  ΛC, accepted w.p. |v−V|/C); elastic 1D momentum exchange. The SAME stream both kicks (rest)
  and drags (moving). Bath MB velocities: inherited from TD02 (CONVERGENCE class inherited).
- **Declared upfront — P4 Stokes–Einstein/FD02 closure: DEFERRED/OPEN.** A 1D shot-noise toy has
  no Stokes hydrodynamics (no 6πηR), and FD02's η itself carries the documented ×1.44 factor.
  Faking a closure number would be worse than owning the gap. Cited dependency: FD02 verdict.

## Phase thresholds (committed)

- **P0 (equilibrium consistency):** ⟨MV²⟩/k_BT = 1 ± 2% (free run, no thermostat on V).
- **P1 (Brownian walk):** MSD linear at t ≫ τ_v; D from MSD slope (fit window [5τ, T_run]);
  VACF decays with measured τ_v reported. Honesty note committed: the VACF decays on the
  *collision* timescale of the toy bath; the prompt's `~t_P` claim is untestable and will be
  classed OPEN, not asserted.
- **P2 (Einstein relation, the core gate):** D/(µk_BT) = 1.00 ± 2% at BOTH T = 1 and T = 4,
  with µ = V_drift/F from the same collision code under constant force (F chosen so
  V_drift ≈ 0.05 bath-thermal units); linearity check: V_drift(F/2)/V_drift(F) = 0.5 ± 0.05.
  Statistics budget: 200 walkers × T_run = 20000 → SE ≈ 1.2% (declared).
- **P3 (FDT spectrum, zero-frequency):** clamped-V force noise S_F(0) = rate·⟨Δp²⟩ vs 2γk_BT
  with γ = F/V_drift from P2: ratio 1 ± 3% at T = 1 (O(m/M) = 1% correction budget declared).
- **P4:** DEFERRED as declared above. No number will be produced.

## Decision rules

- P2 miss > 2%: first check SE and linearity (pivot: smaller F within linear regime, more
  walkers); if a genuine ≠1 persists → report F-class finding (noise and drag not one statistic).
- No noise-amplitude knob exists in the code; if one is ever needed to pass, that IS the failure.
- Physics class: which step would break on a non-SDT substrate?

## Run record

- [x] Pre-compile note: first draft's MSD estimator (endpoint fit) had ~5–10% statistical
      error — above the committed 2% Einstein gate; replaced with disjoint-increment MSD +
      intercept fit and longer runs BEFORE first compile (estimator fix, gates unchanged).
- [x] compile (MSVC /std:c++20 /O2) — clean
- [x] RUN 1: exit 0 — ALL gates pass on the first execution, no adjustments:
      P0 0.9982/0.9994 · P2 Einstein 1.0061 (T=1), 1.0039 (T=4) · P3 FDT 0.9866
      (= declared M/(M+1) correction) · P4 DEFERRED as pre-declared.
- [x] verdict: TD08_VERDICT.md — Prompt B · Physics CONVERGENCE/NULL-SDT ·
      Recovery PARTIAL · Cascade roots FD-ROOT-2 (P4), TD-ROOT-1 (mechanism)
