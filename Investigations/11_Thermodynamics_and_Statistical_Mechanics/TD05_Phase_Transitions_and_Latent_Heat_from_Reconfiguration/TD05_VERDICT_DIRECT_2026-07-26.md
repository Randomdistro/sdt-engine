# TD05 — Direct re-run verdict (2026-07-26)

> **Run class: DIRECT** (main session, no agents). Agent-era `TD05_VERDICT.md`, `RUN_LOG.md`,
> `results.txt`, `td05.exe/.obj` deleted 2026-07-26 (git-recoverable). Tool:
> `td05_packing_reconfiguration.cpp`, g++ 15.2 -O2, **exit 1 — the honest FAIL exit**.
> Output: `td05_rerun_2026-07-26.txt`.

## Prompt contraband check

**CLEAN.** ⓪.1 poses the real fork (native packing threshold vs needing a Landau
order-parameter field); the tool confirms "no order-parameter field entered the dynamics at any
point." Clausius–Clapeyron honestly DEFERRED (producing a slope off-coexistence would be
fabrication — the tool's words).

## What the direct run measured

- **P1 (PASS):** a sharp transition with hysteresis emerges from packing dynamics alone —
  ΔU jump 0.75 ε at T* = 1.88 (6.7× median step), order parameter collapses 0.50 → 0.02 in ≤ 2
  steps, cooling arrests into a glass (FLM09-type, reported).
- **P2 (FAIL, the informative one):** the committed ledger L = N_bonds·Δε ships factor **2.66**
  (gate 1.5). The trace shows why: at fixed density the latent heat is **86% bond RE-PRICING**
  (surviving bonds moved to worse geometry) and only **14% bond count**. The committed
  count×cost form is the errored premise — the Law IV reorganisation cost is real but it is
  priced per-geometry, not per-bond. "L = T_mΔS with calorimetric ΔS is an identity — NOT
  TESTED" disclosed.
- **P3 (PASS):** argon order-of-magnitude — L_melt factor 1.58, L_vap factor 1.04, ordering
  L_vap ≫ L_melt. T* × 119.8 K = 225 K vs T_m = 83.8 K reported only (fixed-ρ superheat, not
  sold as a prediction).

## Verdict

- **Prompt completion: B** (gates run as committed; one fired honestly; one deferred honestly).
- **Physics class: FAIL STANDING on the committed ledger form; mechanism LIVE.** The transition
  *existence*, sharpness, hysteresis, and glass arrest are native packing results. The ledger
  must be re-posed as a **re-pricing ledger** (cost = Σ bonds × geometry-degradation, not
  count × unit-cost) — a corrected premise with a populated failure class, ready for a fresh
  pre-commit. Until that runs, TD05's latent-heat claim stays dead as committed.
- **Recovery:** re-pose available (re-pricing ledger) · **Cascade root:** bond re-pricing =
  the same interleave-geometry cost language as the nuclear boundary work.
