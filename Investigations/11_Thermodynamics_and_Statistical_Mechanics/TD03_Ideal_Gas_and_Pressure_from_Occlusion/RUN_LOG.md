# RUN_LOG — TD03 Ideal Gas and Pressure from Occlusion

**Executor:** J. C. Harvey, Melbourne · 2026-07-03
**Committed BEFORE first compile/run. No threshold below may be widened after a number is seen.**

## Pre-Run Commitments — TD03

- Prompt completion target: **B**
- Physics class hoped: NATIVE for the P1 geometric core; pre-registered suspicion: the wall
  momentum-flux counting is textbook kinetic theory, so overall class likely **CONVERGENCE**
  with one genuinely SDT-specific sub-result — the FLM08 W+1=4 tetrahedral bond set's rank-2
  isotropy (⟨cos²θ⟩ = 1/3 exact for ANY wall orientation, anisotropy deferred to rank 4).
- CALIBRATED budget: 0 in P1/P2. P3 co-volume: the excluded-volume → pressure-correction
  link (virial/Mayer) is **BORROWED-LINK, flagged**; b itself computed by MC integration, no fit.
- Engine namespaces: `measured::k_B`, `lattice_structure` (tetrahedral coordination = law_VI W+1).
- TD02 inheritance: equipartition + MB bath — TD02 landed **CONVERGENCE (firewall not fired)**;
  per the prompt's T4, TD03's PV=Nk_BT therefore carries class ≤ CONVERGENCE. Stated upfront.

## Phase thresholds (committed)

- **P1a (native 1/3 from dynamics):** velocities generated by TD02's pair-rotation mixing
  (NOT drawn from a Gaussian RNG — the bath is made, not assumed). MC ⟨cos²θ⟩ = 1/3 within
  3σ_stat; finite-time wall impulse counting converges to the exact per-particle Σmv_x²/L_x
  accumulation within 0.5% at ≥2000 crossings/particle.
- **P1b (FLM08 discrete bond check):** for the four tetrahedral bond directions
  (coordination = 4 = law_VI W+1): max over 1e5 random wall normals of
  |mean (b·n)² − 1/3| < 1e-14 (rank-2 isotropy EXACT — no inserted factor); rank-4 moment
  mean (b·n)⁴ must span [1/9, 7/27] vs continuum 1/5 — reported as the lattice fingerprint
  location (predicted analytically BEFORE the run from Σbb^T = (4/3)I).
- **P2 (ideal gas):** with T entropic (kT = 2E/3N) the axis-mean PV/(NkT) = Σmv²/2E = 1 is an
  **identity** — caught pre-run and NOT gated (gating it would be an IDENTITY-PASS). Committed
  gate instead: per-axis isotropy of the mixed bath, |PV/(NkT)_axis − 1| < 4√(2/Np) for
  Np ∈ {2000, 20000} × kT ∈ {1, 10}; P from the exact specular accumulation P = Σmv_x²/V
  (derived) with the P1a event-counting check. Class of the NUMBER: inherited ≤ TD02 =
  CONVERGENCE. What TD03 owns: the flux mechanism + the 1/3.
  *(ADJ-0, pre-first-compile: original draft gated the axis-mean at ±1e-3; replaced before
  any run because it is unfalsifiable. Recorded here for transparency.)*
- **P3 (Dalton + co-volume):** two species (mass ratio 4): P_total = ΣP_i within 1%.
  Excluded volume by MC integration: b = 4×(4π/3)r³ within 0.5% MC error, b > 0
  (repulsive/excluded sign — wake-overlap). Virial link flagged BORROWED.
- **P4 (delete test):** every result k_B × pure number; no Z, no h, no ensemble object.

## Decision rules

- P1b rank-2 residual > 1e-14 → that IS a result (frustration residual δ); report as lattice
  fingerprint, do not average away.
- Any gate miss → pivot table of PROMPT.md §⑩; never widen.
- Physics class decided by: which step would break if the substrate were not the SDT lattice?

## Run record

- [x] compile (MSVC /std:c++20 /O2) — clean
- [x] RUN 1: exit 1 — P3 species-split gate FIRED (0.9834 vs 1% gate). Traced: gate committed
      below the √(2/1800)=3.3% noise floor of a 600-particle species. **ADJ-1 (allowed: larger
      bath, gate UNCHANGED at 1%):** N1=200k, N2=100k → noise floor 0.26%.
- [x] RUN 2: exit 0 — all gates pass → results.txt (real stdout)
- [x] verdict: TD03_VERDICT.md — Prompt B · Physics CONVERGENCE (P1b sub-result NATIVE)
