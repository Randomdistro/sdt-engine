# GOM05: Variable Spation Closure — re-earning the c-gradient identities, with the campaign statuses attached

> **Author:** J. C. Harvey, Melbourne. **Status:** SPEC — **first standalone prompt 2026-07-24.**
> The June doc `GOM05_VARIABLE_CLOSURE.md` (SDT-Canonical-Engine byline) REMAINS as the
> spec-source carrying Harvey's framing directives (no attraction; koppa is a velocity quantity;
> the mass-ejection conjecture excluded-pending-compute) — but its "EXECUTED 4/4, 0.0000%"
> claims are June-era and are **re-earned here or not held.** Agent-era output deleted
> 2026-07-24. **Inherits:** template + protocol + HUNTER §0/§G. **Execution: DIRECT ONLY.**
> Tool: `gom05_variable_closure.cpp` (+ `sundiver.js` visual).

## ⓪ The Golden Rule — five questions

1. **What don't we know?** — Which parts of the variable-closure picture are earned at which
   grade, once labels are applied honestly:
   (a) the **triple identity** ℓ_P(r)/ℓ_P,∞ = 1−z, c_local = c(1−z), z = ϟ/r — exact BY
   CONSTRUCTION given ϟ (one definition read three ways): label IDENTITY, re-earn the numbers
   (solar-surface z = 2.123×10⁻⁶, Δc = 636.5 m/s class);
   (b) the **Shapiro re-read** (123.66 µs Earth–Mars) — CONVERGENCE (same number as standard;
   the mechanism story — smaller spations, more relay boundaries per metre — is the native
   reading and stays LINGUISTIC until it forces a difference);
   (c) the **lumiopause** r = √(L☉/4πF_CMB) = 20,857 AU — exact algebra from law_II anchors,
   but its DATA status is campaign-controlled: E106 first test INCONCLUSIVE (osculating
   aphelia; original-1/a retest = MUST_FETCH F3). No confirmation language;
   (d) the **galactic depth floor** (~105 m/s) — E108, DOWNGRADED in the firing log (absolute
   number model-dependent ±50%); carried as a flagged, not banked, prediction;
   (e) the **lumiostasis mechanism** ("resting at the depth surface, not orbiting") — the
   genuinely distinctive claim, falsifier = Oort clustering at the depth surface vs Keplerian
   distribution (E106's retest decides);
   (f) the **stellar-interior mapping** (nucleon column density → local closure → helioseismic
   sound speeds) — the OPEN next step, now better armed via GOM09's rebuilt K/G routes.
2. **Why does it matter?** — GOM05 is the load-bearing strong-field refinement under GOM06 (G1
   c_local), GOM01 (the kernel), and the E102/E106/E108 campaign cluster; its FLM06 dependency
   (ℓ_P = √(ϟ·ƛ) closure) is agent-era-adjacent and must be cited as UNCONFIRMED-class, per the
   whitelist ruling (no Seed-Theorem authority).
3. **How will we find out?** — P1 re-earn (a)+(b) direct with gates 0.001%; P2 print the
   c-gradient table with per-row labels and the two campaign-status lines attached to (c)/(d);
   P3 the lumiostasis falsifier restated with its decision data named (F3); P4 stretch:
   the interior mapping scoped (not run) with its data needs.
4. **What would prove us wrong?** — identity re-earn failing (tool/algebra bug); the Shapiro
   number missing (inputs); E106's original-1/a retest showing clean Keplerian 1/a with no
   depth-surface clustering (lumiostasis KILLED — pre-committed here).
5. **Done =** dual verdict with the six-part label ledger.

## §0 Reader's contract
1. IDENTITY / CONVERGENCE / FLAGGED / OPEN labels per part (a)–(f) on every output line.
2. Campaign statuses are controlling: E106 INCONCLUSIVE, E108 DOWNGRADED — no stronger words.
3. FLM06 dependency cited as agent-era/unconfirmed; the ℓ_P,∞ anchor caveat (one anchor
   unavoidable) stated WITHOUT Seed-Theorem authority (that claim is itself unconfirmed).
4. No G/M; direct only; teed stdout.

## §④ Phases
**P0** — tool audit (which proofs it runs; c/GM sweep). **P1** — identities + Shapiro re-earned:
gates z_surface = 2.123×10⁻⁶ ±0.1%, Δc(surface) = 636.5 ±1 m/s, Shapiro 123.66 ±0.1 µs — all
labeled per contract. **P2** — the gradient table (surface → lumiopause → galactic floor), rows
labeled, campaign statuses printed in-table. **P3** — lumiostasis falsifier: pre-commit
(re-affirming E106-F3): original-1/a distribution shows excess density at the 20,857 AU depth
surface = PASS; smooth Keplerian decline = lumiostasis KILLED. Decision data: MUST_FETCH F3.
**P4** — stretch scope note: interior mapping (data: helioseismic sound-speed profile — add
MUST_FETCH row when opened; method: nucleon column → Δℓ_P(r) → sound-speed prediction vs
helioseismology; links GOM09-P2/P3).

## §⑧ Falsifiers
F1 identity gates miss → bug, STOP. F2 Shapiro miss → inputs. F3 (standing, data-gated): E106
original-1/a clean-Keplerian → lumiostasis KILLED, logged. F4 any confirmation language on
(c)/(d) → verdict VOID.

## §⑤ Outputs
Fresh `RUN_LOG.md` · `gom05_rerun_<date>.txt` · `GOM05_VERDICT_DIRECT_<date>.md` (six-part
ledger). The June doc stays, annotated by the verdict, never edited silently.

## ⑩ Pre-Run Commitments
```markdown
- Target [A|B|C] · ceilings: (a) IDENTITY (b) CONVERGENCE (c,d) FLAGGED (e) falsifier-armed (f) OPEN
- CALIBRATED budget: 0 · Gates: P1 three numbers · P3 pre-commit restated before any F3 data
- Forbidden: confirmation language; Seed-Theorem authority; silent edits to the June doc; agents.
```

*GOM05 · first spec 2026-07-24 · one definition read three ways is an identity; the physics is
in the lumiostasis wager, and the wager's data is named.*
