# RUN_LOG — GOM16 (The Vulcan Ledger — the shaken box)

> Committed BEFORE the tool. Harvey's frame, taken whole: every gravitationally interleaved
> speck is part of the Sun — held and felt by the shadow that oblates and the shadow that
> rotates; the ledger is movement over distance, exact to the baryon. NOTHING here is the
> rival's: no G, no M, no curvature, no borrowed perturbation "theory."

## The WHY (stated first, because it is the mechanism, not a gloss)
Gravity works because every baryon blocks its share of the convergence flow. A body's whole
pull is one length: ϟ = N·ϟ_b (count times per-baryon reach). The acceleration it commands is
a = c²ϟ/r² — movement (c²) times reach over distance squared — derived from shadow geometry
(Law III), read off the sky from any dependent's (a,T). Shadows ADD (the ledger); an oblate
spinning body casts an oblate spinning shadow (its form-term); an interior speck adds its
shadow to Mercury's sky. An orbit precesses when the summed shadow deviates from pure 1/r² —
apsidal drift is CALCULUS ON THE NATIVE FORCE LAW, owed to no rival.

## Pre-Run Commitments
- Inputs to the box ("figures in"): kinematic koppas only (moons' (a,T); lander g·R²/c² for
  moonless bodies); Mercury's (a, e, T); the Sun's measured form-oblateness (helioseismic
  shape parameter, declared informational-measured); supplementary-precession residual bounds
  |Δϖ̇| — DECLARED from reference memory at two levels (conservative 0.03″/cy, aggressive
  0.005″/cy, MESSENGER-ephemeris class; re-verify against primary INPOP/EPM before any
  publication claim).
- The box's outputs: (1) the apsidal-drift response of Mercury to an interior shadow of reach
  ϟ_v at radius a_v — derived numerically from the native force law by direct orbit
  integration (no closed-form borrowed; the integrator IS the derivation); (2) the inversion:
  ϟ_v ceiling vs a_v across 0.06–0.21 AU; (3) the baryon count N = ϟ_v/ϟ_b (ϟ_b = 1.2423e-54
  m — the ONE seed anchor, declared per FLM13); (4) composition classes by crystallographic
  packing, kg-free (atoms/m³ from lattice constants × A): iron 4.75e30 baryons/m³,
  silicate ≈ 1.97e30, ice ≈ 0.62e30 — with survival physics at 0.06–0.21 AU (600–1500 K:
  ice excluded, silicate marginal inward of ~0.1 AU, iron survives) → the standing
  composition PREDICTION for any true Vulcan; (5) the discriminator: any announced (a_v,
  size s) → koppa ceiling → mean baryon density → composition class, immediately.
- Verification gate: the integrator must reproduce a null (no interior shadow → zero secular
  drift beyond numerical floor) and recover the depth-closure 42.98″/cy when the depth term
  is switched on (GOM08 anchor) before any Vulcan inversion is trusted.
- Honesty: I cannot verify any announced detection; no detection parameters are assumed. All
  memory-sourced inputs flagged. A found Vulcan violating the density ceiling FALSIFIES the
  closed-ledger premise at the stated bound — the prediction cuts both ways.

- **ADJ-1 (post-run-1, numerics only):** run-1 FIRED its own gates (null floor −2715″/cy;
  linearity 5.40 vs 3.33; cross-check ×8 off) — table discarded, recorded in results. Fix:
  DIFFERENTIAL drift (kv-run minus null-run, identical stepping — floor cancels to first
  order) + finer steps. No physics, thresholds, or definitions changed.
