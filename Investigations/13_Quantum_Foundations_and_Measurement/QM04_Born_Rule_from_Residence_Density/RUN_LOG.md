# RUN_LOG — QM04 Born Rule from Residence Density

**J. C. Harvey, Melbourne — 2026-07-04.** Executor: HUNTER-disciplined agent run.
Written BEFORE any tool code, per `PROMPT_EXECUTION_PROTOCOL.md`.

## Scope ruling (from the tasking, logged up front)

The residence law (Phase 2's core: landing count ∝ wake energy density) is OWNED by **FLM14
rule-form-4, in flight**. This run does the **analytically-earnable part** (the elastic square,
Phase 1; normalisation/positivity, Phase 3; and one pre-registered NEGATIVE demonstration that
sharpens what FLM14 must deliver) and **DEFERS** everything that needs rule-form-4's output.
The known QM04 trap (the prompt's anti-tautology firewall): residence→Born can smuggle |ψ|² in by
*assigning* landing probability = energy density. Nothing below assigns any probability.

## Pre-Run Commitments — QM04

- Prompt completion target: **C** (Phase 1 + 3 earned; Phase 2 deferred by ruling).
- Physics class hoped: NATIVE on the elastic square; OPEN/DEFERRED on residence.
- CALIBRATED budget: 0. No Born rule, no ψ, no probability axiom anywhere in the code. The field
  code may contain ONLY real travelling carriers cos(kr − ωt); `cos²`, `|amp|²`, and any
  probability assignment are forbidden in the field/steering chain (firewall).
- §G mirror: faith ledger — accepted: elastic energy ∝ displacement² (Law-I strain machinery,
  internal consistency; same weight as QM02). Gatekeeper — no rival axiom in any gate.

## Phase thresholds (committed before run)

- **P1 (the square is elastic energy — machinery):** two real Huygens carriers from two slits,
  summed as REAL time-varying fields, energy = time-average of (total displacement)². Gates:
  (a) measured cross-term = A_aA_b·cos(kΔr) to RMS < 0.1% of peak intensity;
  (b) equal-amplitude fringe shape = cos²(kΔr/2)·(envelope) to RMS < 0.1% — with cos² NEVER
  written into the field code (it must emerge from the time-average of summed real carriers);
  (c) the cross-term's sign flips where Δr crosses λ/2 (interference minima real, not coded).
- **P2-neg (pre-registered NEGATIVE — the firewall in action):** steer walkers from a uniform
  start by naive gradient-ascent on the wake energy density ("go where convergence is greatest").
  Committed expectation: the landing histogram does NOT reproduce the energy density (walkers
  pile onto fringe maxima; RMS vs I expected > 20%). Purpose: prove the residence law is NOT
  generic — "any least-resistance steering gives Born" is hereby killed as a hand-wave; the law
  FLM14 must produce is specific. Forbidden after seeing this fail: assigning P = I and calling
  it a measurement (that is the trap the prompt firewalls).
- **P3 (normalisation & positivity):** structural: every walker lands exactly once (count
  conservation ⇒ ΣP = 1); energy density ≥ 0 (⇒ P ≥ 0). Earned analytically, explicitly
  CONDITIONAL on the deferred Phase 2.
- **DEFERRED to FLM14 rule-form-4:** the actual residence∝energy-density demonstration; the QM01
  landing-count fringe; the QM05 joint weighting; the QM07 re-preparation density. One root.

## Run record

- 2026-07-04: commitments frozen. Tool `qm04_born_residence.py` written after this block.
  → `qm04_results.txt` (real stdout), `QM04_VERDICT.md`.
